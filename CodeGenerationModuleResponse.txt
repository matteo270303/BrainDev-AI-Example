Codice per il database generato
{
  "codice": "[{\n  \"nome_file\": \"db_schema.sql\",\n  \"codice\": \"-- DB-01: Create and use the main database\\nCREATE DATABASE IF NOT EXISTS personal_portfolio CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;\\n\\nUSE personal_portfolio;\\n\\n-- DB-02: Create the 'users' table\\nCREATE TABLE users (\\n    id INT AUTO_INCREMENT PRIMARY KEY,\\n    username VARCHAR(80) UNIQUE NOT NULL,\\n    password_hash VARCHAR(255) NOT NULL\\n) ENGINE=InnoDB;\\n\\n-- DB-03: Create the 'articles' table\\nCREATE TABLE articles (\\n    id INT AUTO_INCREMENT PRIMARY KEY,\\n    title VARCHAR(255) NOT NULL,\\n    slug VARCHAR(255) UNIQUE NOT NULL,\\n    excerpt TEXT,\\n    content_markdown MEDIUMTEXT NOT NULL,\\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\\n    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP\\n) ENGINE=InnoDB;\\n\\n-- DB-06: Create the 'projects' table\\nCREATE TABLE projects (\\n    id INT AUTO_INCREMENT PRIMARY KEY,\\n    title VARCHAR(255) NOT NULL,\\n    slug VARCHAR(255) UNIQUE NOT NULL,\\n    description TEXT NOT NULL,\\n    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,\\n    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP ON UPDATE CURRENT_TIMESTAMP\\n) ENGINE=InnoDB;\\n\\n-- DB-07: Create the 'project_images' table\\nCREATE TABLE project_images (\\n    id INT AUTO_INCREMENT PRIMARY KEY,\\n    project_id INT NOT NULL,\\n    image_url VARCHAR(255) NOT NULL,\\n    alt_text VARCHAR(255),\\n    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE\\n) ENGINE=InnoDB;\\n\\n-- DB-08: Create the 'project_links' table\\nCREATE TABLE project_links (\\n    id INT AUTO_INCREMENT PRIMARY KEY,\\n    project_id INT NOT NULL,\\n    link_text VARCHAR(100) NOT NULL,\\n    url VARCHAR(255) NOT NULL,\\n    FOREIGN KEY (project_id) REFERENCES projects(id) ON DELETE CASCADE\\n) ENGINE=InnoDB;\\n\\n-- DB-04: Create the 'tags' table\\nCREATE TABLE tags (\\n    id INT AUTO_INCREMENT PRIMARY KEY,\\n    name VARCHAR(100) UNIQUE NOT NULL,\\n    slug VARCHAR(100) UNIQUE NOT NULL\\n) ENGINE=InnoDB;\\n\\n-- DB-05: Create the 'article_tags' pivot table\\nCREATE TABLE article_tags (\\n    article_id INT NOT NULL,\\n    tag_id INT NOT NULL,\\n    PRIMARY KEY (article_id, tag_id),\\n    FOREIGN KEY (article_id) REFERENCES articles(id) ON DELETE CASCADE,\\n    FOREIGN KEY (tag_id) REFERENCES tags(id) ON DELETE CASCADE\\n) ENGINE=InnoDB;\\n\\n-- DB-09: Create the 'site_content' table\\nCREATE TABLE site_content (\\n    id INT AUTO_INCREMENT PRIMARY KEY,\\n    content_key VARCHAR(100) UNIQUE NOT NULL,\\n    content_value TEXT NOT NULL\\n) ENGINE=InnoDB;\\n\\n-- Optimization: Add indexes for better performance\\n-- Indexes on columns with UNIQUE constraints (slug, name, content_key) are created automatically.\\n\\nCREATE INDEX idx_project_images_project_id ON project_images(project_id);\\nCREATE INDEX idx_project_links_project_id ON project_links(project_id);\\nCREATE INDEX idx_article_tags_tag_id ON article_tags(tag_id); -- For finding all articles associated with a tag\\n\"\n},{\n  \"nome_file\": \"db_seed.sql\",\n  \"codice\": \"-- This file contains initial data for the application.\\n-- Use this script after the schema has been created.\\n\\n-- DB-10: Generate a query to insert an initial administrator user\\n-- NOTE: The password hash provided here is a placeholder for 'strongpassword'.\\n-- In a real application, this hash should be generated securely by the backend logic.\\nINSERT INTO users (username, password_hash) \\nVALUES ('admin', 'pbkdf2:sha256:600000$Wv9aYq3ZbrB2j8tN$3dd8d3b8f2a5e4c6e9d7ac3f29ac1d7a861d9e26e3c0f4d38c6c4c0b3c6b2b1a');\\n\\n-- DB-11: Generate a query to populate the 'site_content' table with default 'About Me' content\\nINSERT INTO site_content (content_key, content_value) \\nVALUES ('about_me_content', 'This is the default content for the About Me page. It can be edited by the administrator in the admin panel.');\\n\"\n}]",
  "nome_file": "JSON"
}
[Codice Backend]
Codice per il backend generato
{
  "files": [
    {
      "codice": "from app.extensions import db\nfrom werkzeug.security import generate_password_hash, check_password_hash\nfrom sqlalchemy.sql import func\n\n# Association table for the Many-to-Many relationship between Article and Tag\narticle_tags = db.Table('article_tags',\n    db.Column('article_id', db.Integer, db.ForeignKey('articles.id', ondelete='CASCADE'), primary_key=True),\n    db.Column('tag_id', db.Integer, db.ForeignKey('tags.id', ondelete='CASCADE'), primary_key=True)\n)\n\nclass User(db.Model):\n    __tablename__ = 'users'\n    id = db.Column(db.Integer, primary_key=True)\n    username = db.Column(db.String(80), unique=True, nullable=False)\n    password_hash = db.Column(db.String(255), nullable=False)\n\n    def set_password(self, password):\n        self.password_hash = generate_password_hash(password)\n\n    def check_password(self, password):\n        return check_password_hash(self.password_hash, password)\n\n    def __repr__(self):\n        return f'<User {self.username}>'\n\nclass Article(db.Model):\n    __tablename__ = 'articles'\n    id = db.Column(db.Integer, primary_key=True)\n    title = db.Column(db.String(255), nullable=False)\n    slug = db.Column(db.String(255), unique=True, nullable=False)\n    excerpt = db.Column(db.Text, nullable=True)\n    content_markdown = db.Column(db.Text, nullable=False) # Changed from MEDIUMTEXT for portability\n    created_at = db.Column(db.TIMESTAMP, server_default=func.now())\n    updated_at = db.Column(db.TIMESTAMP, server_default=func.now(), onupdate=func.now())\n\n    tags = db.relationship('Tag', secondary=article_tags, back_populates='articles', lazy='dynamic')\n\n    def __repr__(self):\n        return f'<Article {self.title}>'\n\nclass Tag(db.Model):\n    __tablename__ = 'tags'\n    id = db.Column(db.Integer, primary_key=True)\n    name = db.Column(db.String(100), unique=True, nullable=False)\n    slug = db.Column(db.String(100), unique=True, nullable=False)\n\n    articles = db.relationship('Article', secondary=article_tags, back_populates='tags', lazy='dynamic')\n\n    def __repr__(self):\n        return f'<Tag {self.name}>'\n\nclass Project(db.Model):\n    __tablename__ = 'projects'\n    id = db.Column(db.Integer, primary_key=True)\n    title = db.Column(db.String(255), nullable=False)\n    slug = db.Column(db.String(255), unique=True, nullable=False)\n    description = db.Column(db.Text, nullable=False)\n    created_at = db.Column(db.TIMESTAMP, server_default=func.now())\n    updated_at = db.Column(db.TIMESTAMP, server_default=func.now(), onupdate=func.now())\n\n    images = db.relationship('ProjectImage', back_populates='project', cascade='all, delete-orphan')\n    links = db.relationship('ProjectLink', back_populates='project', cascade='all, delete-orphan')\n\n    def __repr__(self):\n        return f'<Project {self.title}>'\n\nclass ProjectImage(db.Model):\n    __tablename__ = 'project_images'\n    id = db.Column(db.Integer, primary_key=True)\n    project_id = db.Column(db.Integer, db.ForeignKey('projects.id', ondelete='CASCADE'), nullable=False)\n    image_url = db.Column(db.String(255), nullable=False)\n    alt_text = db.Column(db.String(255), nullable=True)\n\n    project = db.relationship('Project', back_populates='images')\n\n    def __repr__(self):\n        return f'<ProjectImage {self.image_url}>'\n\nclass ProjectLink(db.Model):\n    __tablename__ = 'project_links'\n    id = db.Column(db.Integer, primary_key=True)\n    project_id = db.Column(db.Integer, db.ForeignKey('projects.id', ondelete='CASCADE'), nullable=False)\n    link_text = db.Column(db.String(100), nullable=False)\n    url = db.Column(db.String(255), nullable=False)\n\n    project = db.relationship('Project', back_populates='links')\n\n    def __repr__(self):\n        return f'<ProjectLink {self.link_text}>'\n\nclass SiteContent(db.Model):\n    __tablename__ = 'site_content'\n    id = db.Column(db.Integer, primary_key=True)\n    content_key = db.Column(db.String(100), unique=True, nullable=False)\n    content_value = db.Column(db.Text, nullable=False)\n\n    def __repr__(self):\n        return f'<SiteContent {self.content_key}>'\n",
      "folder": "app/classes",
      "nome_file": "models.py"
    },
    {
      "codice": "# This file can be left empty",
      "folder": "app/functions",
      "nome_file": "__init__.py"
    },
    {
      "codice": "import os\nimport re\nimport secrets\nimport unicodedata\n\nfrom flask import current_app\n\ndef slugify(value):\n    \"\"\"\n    Normalizes string, converts to lowercase, removes non-alpha characters,\n    and converts spaces to hyphens.\n    \"\"\"\n    if not value:\n        return \"\"\n    value = str(value)\n    value = unicodedata.normalize('NFKD', value).encode('ascii', 'ignore').decode('ascii')\n    value = re.sub(r'[^\\w\\s-]', '', value).strip().lower()\n    value = re.sub(r'[-\\s]+', '-', value)\n    value = re.sub(r'^-+|-+$', '', value)\n    return value\n\ndef save_picture(form_picture):\n    \"\"\"\n    Saves an uploaded picture with a secure, random filename.\n    Returns the filename of the saved picture.\n    \"\"\"\n    random_hex = secrets.token_hex(8)\n    _, f_ext = os.path.splitext(form_picture.filename)\n    picture_fn = random_hex + f_ext\n    \n    # The path is constructed relative to the project's root 'backend' directory.\n    # current_app.root_path -> /path/to/backend/app\n    # os.path.join(current_app.root_path, '..') -> /path/to/backend\n    upload_path = os.path.join(current_app.root_path, '..', current_app.config['UPLOAD_FOLDER'])\n    \n    if not os.path.exists(upload_path):\n        os.makedirs(upload_path)\n\n    picture_path = os.path.join(upload_path, picture_fn)\n    form_picture.save(picture_path)\n    \n    return picture_fn\n\ndef delete_picture(filename):\n    \"\"\"\n    Deletes a picture from the upload folder.\n    \"\"\"\n    if not filename:\n        return\n\n    try:\n        picture_path = os.path.join(current_app.root_path, '..', current_app.config['UPLOAD_FOLDER'], filename)\n        if os.path.exists(picture_path):\n            os.remove(picture_path)\n    except Exception as e:\n        current_app.logger.error(f\"Error deleting file {filename}: {e}\")\n",
      "folder": "app/functions",
      "nome_file": "helpers.py"
    },
    {
      "codice": "from flask import Blueprint, jsonify, request\nfrom app.classes.models import Article, Project, SiteContent, Tag\n\npublic_bp = Blueprint('public_bp', __name__, url_prefix='/api')\n\n@public_bp.route('/site-content/about-me', methods=['GET'])\ndef get_about_me_content():\n    \"\"\"Retrieves the content for the 'About Me' page.\"\"\"\n    content = SiteContent.query.filter_by(content_key='about_me_content').first()\n    if not content:\n        return jsonify({'message': 'Content not found'}), 404\n    return jsonify({'content': content.content_value})\n\n@public_bp.route('/projects', methods=['GET'])\ndef get_projects():\n    \"\"\"Returns a paginated list of all projects.\"\"\"\n    page = request.args.get('page', 1, type=int)\n    per_page = request.args.get('per_page', 10, type=int)\n    \n    projects_pagination = Project.query.order_by(Project.created_at.desc()).paginate(page=page, per_page=per_page, error_out=False)\n    projects = projects_pagination.items\n    \n    result = []\n    for project in projects:\n        result.append({\n            'id': project.id,\n            'title': project.title,\n            'slug': project.slug,\n            'excerpt': project.description[:150] + '...' if len(project.description) > 150 else project.description\n        })\n        \n    return jsonify({\n        'projects': result,\n        'total': projects_pagination.total,\n        'pages': projects_pagination.pages,\n        'current_page': projects_pagination.page\n    })\n\n@public_bp.route('/projects/<string:slug>', methods=['GET'])\ndef get_project_by_slug(slug):\n    \"\"\"Returns the full details of a single project by its slug.\"\"\"\n    project = Project.query.filter_by(slug=slug).first()\n    if not project:\n        return jsonify({'message': 'Project not found'}), 404\n\n    project_data = {\n        'id': project.id,\n        'title': project.title,\n        'slug': project.slug,\n        'description': project.description,\n        'created_at': project.created_at.isoformat(),\n        'images': [{'id': img.id, 'url': img.image_url, 'alt_text': img.alt_text} for img in project.images],\n        'links': [{'id': link.id, 'text': link.link_text, 'url': link.url} for link in project.links]\n    }\n    return jsonify(project_data)\n\n@public_bp.route('/articles', methods=['GET'])\ndef get_articles():\n    \"\"\"Returns a paginated list of all articles.\"\"\"\n    page = request.args.get('page', 1, type=int)\n    per_page = request.args.get('per_page', 10, type=int)\n    \n    articles_pagination = Article.query.order_by(Article.created_at.desc()).paginate(page=page, per_page=per_page, error_out=False)\n    articles = articles_pagination.items\n    \n    result = []\n    for article in articles:\n        result.append({\n            'id': article.id,\n            'title': article.title,\n            'slug': article.slug,\n            'excerpt': article.excerpt,\n            'created_at': article.created_at.isoformat(),\n            'tags': [{'name': tag.name, 'slug': tag.slug} for tag in article.tags]\n        })\n        \n    return jsonify({\n        'articles': result,\n        'total': articles_pagination.total,\n        'pages': articles_pagination.pages,\n        'current_page': articles_pagination.page\n    })\n\n@public_bp.route('/articles/<string:slug>', methods=['GET'])\ndef get_article_by_slug(slug):\n    \"\"\"Returns the full content of a single article by its slug.\"\"\"\n    article = Article.query.filter_by(slug=slug).first()\n    if not article:\n        return jsonify({'message': 'Article not found'}), 404\n\n    article_data = {\n        'id': article.id,\n        'title': article.title,\n        'slug': article.slug,\n        'content_markdown': article.content_markdown,\n        'excerpt': article.excerpt,\n        'created_at': article.created_at.isoformat(),\n        'tags': [{'name': tag.name, 'slug': tag.slug} for tag in article.tags]\n    }\n    return jsonify(article_data)\n\n@public_bp.route('/tags', methods=['GET'])\ndef get_tags():\n    \"\"\"Returns a list of all available tags.\"\"\"\n    tags = Tag.query.all()\n    return jsonify([{'id': tag.id, 'name': tag.name, 'slug': tag.slug} for tag in tags])\n",
      "folder": "app/api",
      "nome_file": "public_routes.py"
    },
    {
      "codice": "from flask import Blueprint, request, jsonify\nfrom flask_jwt_extended import create_access_token\nfrom app.classes.models import User\n\nauth_bp = Blueprint('auth_bp', __name__, url_prefix='/api/auth')\n\n@auth_bp.route('/login', methods=['POST'])\ndef login():\n    \"\"\"Authenticates an administrator and returns a JWT access token.\"\"\"\n    data = request.get_json()\n    if not data or not data.get('username') or not data.get('password'):\n        return jsonify({'message': 'Username and password are required'}), 400\n\n    username = data.get('username')\n    password = data.get('password')\n\n    user = User.query.filter_by(username=username).first()\n\n    if user and user.check_password(password):\n        access_token = create_access_token(identity=user.id)\n        return jsonify(access_token=access_token)\n    else:\n        return jsonify({'message': 'Invalid credentials'}), 401\n",
      "folder": "app/api",
      "nome_file": "auth_routes.py"
    },
    {
      "codice": "import json\nfrom flask import Blueprint, request, jsonify, current_app\nfrom flask_jwt_extended import jwt_required\n\nfrom app.extensions import db\nfrom app.classes.models import Article, Project, SiteContent, Tag, ProjectImage, ProjectLink\nfrom app.functions.helpers import slugify, save_picture, delete_picture\n\nadmin_bp = Blueprint('admin_bp', __name__, url_prefix='/api/admin')\n\n# --- Site Content Management ---\n@admin_bp.route('/site-content/about-me', methods=['PUT'])\n@jwt_required()\ndef update_about_me():\n    data = request.get_json()\n    if 'content' not in data:\n        return jsonify({'message': 'Missing content field'}), 400\n\n    content_entry = SiteContent.query.filter_by(content_key='about_me_content').first()\n    if not content_entry:\n        return jsonify({'message': 'Content key not found'}), 404\n\n    content_entry.content_value = data['content']\n    db.session.commit()\n    return jsonify({'message': 'About Me content updated successfully', 'content': content_entry.content_value})\n\n# --- Project Management (CRUD) ---\n@admin_bp.route('/projects', methods=['POST'])\n@jwt_required()\ndef create_project():\n    data = request.form\n    files = request.files.getlist('images')\n\n    if not data.get('title') or not data.get('description'):\n        return jsonify({'message': 'Title and description are required'}), 400\n\n    new_project = Project(\n        title=data['title'],\n        slug=slugify(data['title']),\n        description=data['description']\n    )\n    db.session.add(new_project)\n    \n    if data.get('links'):\n        try:\n            links_data = json.loads(data['links'])\n            for link_item in links_data:\n                new_link = ProjectLink(link_text=link_item['text'], url=link_item['url'])\n                new_project.links.append(new_link)\n        except (json.JSONDecodeError, TypeError):\n             return jsonify({'message': 'Invalid format for links'}), 400\n\n    for file in files:\n        filename = save_picture(file)\n        image_url = f'/uploads/{filename}' \n        new_image = ProjectImage(image_url=image_url, alt_text=data.get('alt_text', ''))\n        new_project.images.append(new_image)\n\n    db.session.commit()\n    return jsonify({'id': new_project.id, 'message': 'Project created successfully'}), 201\n\n@admin_bp.route('/projects/<int:id>', methods=['PUT'])\n@jwt_required()\ndef update_project(id):\n    project = Project.query.get_or_404(id)\n    data = request.get_json()\n\n    if 'title' in data:\n        project.title = data['title']\n        project.slug = slugify(data['title'])\n    if 'description' in data:\n        project.description = data['description']\n    \n    # Note: Image and Link updates would require more complex logic\n    # (e.g., separate endpoints or logic to identify which to add/delete)\n\n    db.session.commit()\n    return jsonify({'id': project.id, 'message': 'Project updated successfully'})\n\n@admin_bp.route('/projects/<int:id>', methods=['DELETE'])\n@jwt_required()\ndef delete_project(id):\n    project = Project.query.get_or_404(id)\n    \n    for image in project.images:\n        filename = image.image_url.split('/')[-1]\n        delete_picture(filename)\n\n    db.session.delete(project)\n    db.session.commit()\n    return jsonify({'message': 'Project deleted successfully'})\n\n# --- Article Management (CRUD) ---\n@admin_bp.route('/articles', methods=['POST'])\n@jwt_required()\ndef create_article():\n    data = request.get_json()\n    if not data or not data.get('title') or not data.get('content_markdown'):\n        return jsonify({'message': 'Missing title or content'}), 400\n\n    new_article = Article(\n        title=data['title'],\n        slug=slugify(data['title']),\n        content_markdown=data['content_markdown'],\n        excerpt=data.get('excerpt', '')\n    )\n\n    if 'tags' in data and isinstance(data['tags'], list):\n        for tag_name in data['tags']:\n            tag = Tag.query.filter_by(name=tag_name).first()\n            if not tag:\n                tag = Tag(name=tag_name, slug=slugify(tag_name))\n                db.session.add(tag)\n            new_article.tags.append(tag)\n    \n    db.session.add(new_article)\n    db.session.commit()\n    return jsonify({'id': new_article.id, 'message': 'Article created successfully'}), 201\n\n@admin_bp.route('/articles/<int:id>', methods=['PUT'])\n@jwt_required()\ndef update_article(id):\n    article = Article.query.get_or_404(id)\n    data = request.get_json()\n\n    if 'title' in data:\n        article.title = data['title']\n        article.slug = slugify(data['title'])\n    if 'content_markdown' in data:\n        article.content_markdown = data['content_markdown']\n    if 'excerpt' in data:\n        article.excerpt = data['excerpt']\n\n    if 'tags' in data and isinstance(data['tags'], list):\n        article.tags.clear()\n        for tag_name in data['tags']:\n            tag = Tag.query.filter_by(name=tag_name).first()\n            if not tag:\n                tag = Tag(name=tag_name, slug=slugify(tag_name))\n                db.session.add(tag)\n            article.tags.append(tag)\n\n    db.session.commit()\n    return jsonify({'id': article.id, 'message': 'Article updated successfully'})\n\n@admin_bp.route('/articles/<int:id>', methods=['DELETE'])\n@jwt_required()\ndef delete_article(id):\n    article = Article.query.get_or_404(id)\n    db.session.delete(article)\n    db.session.commit()\n    return jsonify({'message': 'Article deleted successfully'})\n\n# --- Generic Image Upload ---\n@admin_bp.route('/upload-image', methods=['POST'])\n@jwt_required()\ndef upload_image():\n    if 'image' not in request.files:\n        return jsonify({'message': 'No image file provided'}), 400\n    \n    file = request.files['image']\n    if file.filename == '':\n        return jsonify({'message': 'No selected file'}), 400\n    \n    try:\n        filename = save_picture(file)\n        image_url = f'/uploads/{filename}'\n        return jsonify({'url': image_url})\n    except Exception as e:\n        current_app.logger.error(f'Image upload failed: {e}')\n        return jsonify({'message': 'Image upload failed'}), 500\n",
      "folder": "app/api",
      "nome_file": "admin_routes.py"
    },
    {
      "codice": "import os\nfrom dotenv import load_dotenv\n\nload_dotenv()\n\nclass Config:\n    \"\"\"Set Flask configuration from .env file.\"\"\"\n\n    # General Config\n    SECRET_KEY = os.environ.get('SECRET_KEY')\n    FLASK_APP = os.environ.get('FLASK_APP')\n    FLASK_ENV = os.environ.get('FLASK_ENV')\n\n    # Database\n    SQLALCHEMY_DATABASE_URI = os.environ.get('DATABASE_URI')\n    SQLALCHEMY_TRACK_MODIFICATIONS = False\n\n    # JWT\n    JWT_SECRET_KEY = os.environ.get('JWT_SECRET_KEY')\n    \n    # File Uploads\n    UPLOAD_FOLDER = os.environ.get('UPLOAD_FOLDER', 'app/uploads')\n",
      "folder": "",
      "nome_file": "config.py"
    },
    {
      "codice": "Flask==3.1.1\nFlask-SQLAlchemy==3.1.1\nSQLAlchemy==2.0.41\nmysql-connector-python==8.4.0\nFlask-JWT-Extended==4.6.0\nFlask-Cors==4.0.1\npython-dotenv==1.0.1\nWerkzeug==3.0.3\n",
      "folder": "",
      "nome_file": "requirements.txt"
    },
    {
      "codice": "from flask_sqlalchemy import SQLAlchemy\nfrom flask_jwt_extended import JWTManager\nfrom flask_cors import CORS\n\ndb = SQLAlchemy()\njwt = JWTManager()\ncors = CORS()\n",
      "folder": "app",
      "nome_file": "extensions.py"
    },
    {
      "codice": "from flask import Flask, jsonify\nfrom app.extensions import db, jwt, cors\nfrom app.api.public_routes import public_bp\nfrom app.api.admin_routes import admin_bp\nfrom app.api.auth_routes import auth_bp\nfrom config import Config\n\ndef create_app(config_class=Config):\n    \"\"\"Application factory pattern\"\"\"\n    app = Flask(__name__)\n    app.config.from_object(config_class)\n\n    # Initialize Flask extensions\n    db.init_app(app)\n    jwt.init_app(app)\n    cors.init_app(app, resources={r\"/api/*\": {\"origins\": \"*\"}})\n\n    # Register Blueprints\n    app.register_blueprint(public_bp)\n    app.register_blueprint(admin_bp)\n    app.register_blueprint(auth_bp)\n\n    # Error Handlers\n    @app.errorhandler(404)\n    def not_found_error(error):\n        return jsonify({\"message\": \"Resource not found\"}), 404\n\n    @app.errorhandler(500)\n    def internal_error(error):\n        return jsonify({\"message\": \"Internal server error\"}), 500\n\n    return app\n",
      "folder": "app",
      "nome_file": "__init__.py"
    },
    {
      "codice": "from app import create_app\nfrom dotenv import load_dotenv\n\n# Load environment variables from .env file\nload_dotenv()\n\napp = create_app()\n\nif __name__ == '__main__':\n    app.run(host='0.0.0.0', port=5000)\n",
      "folder": "",
      "nome_file": "run.py"
    },
    {
      "codice": "",
      "folder": "app/api",
      "nome_file": "__init__.py"
    },
    {
      "codice": "# This file can be left empty",
      "folder": "app/classes",
      "nome_file": "__init__.py"
    },
    {
      "codice": "",
      "folder": "app/functions",
      "nome_file": "__init__.py"
    }
  ]
}
[Codice Frontend]
Codice per il frontend generato
{
  "files": [
    {
      "codice": "'use client'\n\nimport React from 'react';\nimport Link from 'next/link';\nimport { Bot, Home, User, Rss, Layers } from 'lucide-react';\n\nconst PublicLayoutComponent = ({ children }) => {\n  return (\n    <div className=\"min-h-screen bg-gray-50 text-gray-800 flex flex-col\">\n      <header className=\"bg-white/80 backdrop-blur-md sticky top-0 z-40 border-b border-gray-200\">\n        <div className=\"container mx-auto px-4 sm:px-6 lg:px-8\">\n          <div className=\"flex items-center justify-between h-16\">\n            <div className=\"flex items-center\">\n              <Link href=\"/\" className=\"flex items-center space-x-2 text-xl font-bold text-gray-900\">\n                <Bot className=\"h-7 w-7 text-blue-600\" />\n                <span>MyPortfolio</span>\n              </Link>\n            </div>\n            <nav className=\"hidden md:flex items-center space-x-8 text-sm font-medium\">\n              <Link href=\"/\" className=\"text-gray-600 hover:text-blue-600 transition-colors flex items-center gap-2\"><Home className=\"w-4 h-4\" /> Home</Link>\n              <Link href=\"/about\" className=\"text-gray-600 hover:text-blue-600 transition-colors flex items-center gap-2\"><User className=\"w-4 h-4\" /> About Me</Link>\n              <Link href=\"/projects\" className=\"text-gray-600 hover:text-blue-600 transition-colors flex items-center gap-2\"><Layers className=\"w-4 h-4\"/> Projects</Link>\n              <Link href=\"/articles\" className=\"text-gray-600 hover:text-blue-600 transition-colors flex items-center gap-2\"><Rss className=\"w-4 h-4\"/> Articles</Link>\n            </nav>\n            <div className=\"flex items-center\">\n              \n            </div>\n          </div>\n        </div>\n      </header>\n\n      <main className=\"flex-grow container mx-auto px-4 sm:px-6 lg:px-8 py-8 md:py-12\">\n        {children}\n      </main>\n\n      <footer className=\"bg-white border-t border-gray-200\">\n        <div className=\"container mx-auto px-4 sm:px-6 lg:px-8 py-6 text-center text-sm text-gray-500\">\n          <p>&copy; {new Date().getFullYear()} [Your Name]. All Rights Reserved.</p>\n          <p className=\"mt-1\">Built with Next.js and Flask.</p>\n        </div>\n      </footer>\n    </div>\n  );\n};\n\nexport default PublicLayoutComponent;\n",
      "folder": "app/components",
      "nome_file": "PublicLayoutComponent.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React from 'react';\nimport Link from 'next/link';\nimport { Calendar, Tag as TagIcon } from 'lucide-react';\n\nconst ArticleCardComponent = ({ article }) => {\n  const { slug, title, excerpt, created_at, tags } = article;\n  \n  const formattedDate = new Date(created_at).toLocaleDateString('en-US', {\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric',\n  });\n\n  return (\n    <div className=\"bg-white rounded-xl shadow-sm hover:shadow-md transition-shadow duration-300 border border-gray-100 overflow-hidden\">\n      <div className=\"p-6\">\n        <div className=\"mb-4\">\n          {tags && tags.length > 0 && (\n            <div className=\"flex flex-wrap gap-2 mb-2\">\n              {tags.map(tag => (\n                <span key={tag.slug} className=\"text-xs font-medium text-blue-700 bg-blue-100 rounded-full px-3 py-1 flex items-center gap-1\">\n                 <TagIcon className=\"w-3 h-3\" /> {tag.name}\n                </span>\n              ))}\n            </div>\n          )}\n          <Link href={`/articles/${slug}`}>\n            <h3 className=\"text-xl font-bold text-gray-900 hover:text-blue-600 transition-colors\">{title}</h3>\n          </Link>\n        </div>\n        <p className=\"text-gray-600 text-sm mb-4 line-clamp-3\">{excerpt}</p>\n        <div className=\"flex items-center text-xs text-gray-500\">\n          <Calendar className=\"w-4 h-4 mr-2\" />\n          <span>{formattedDate}</span>\n        </div>\n      </div>\n    </div>\n  );\n};\n\nexport default ArticleCardComponent;\n",
      "folder": "app/components",
      "nome_file": "ArticleCardComponent.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\nimport React from 'react';\nimport Link from 'next/link';\nimport { ArrowRight, Layers } from 'lucide-react';\n\nconst ProjectCardComponent = ({ project }) => {\n  const { slug, title, excerpt, images } = project;\n  const imageUrl = images && images.length > 0 ? `http://localhost:5000${images[0].url}` : null;\n\n  return (\n    <Link href={`/projects/${slug}`} className=\"block h-full\">\n      <div className=\"group bg-white rounded-xl shadow-sm hover:shadow-lg transition-all duration-300 border border-gray-100 overflow-hidden h-full flex flex-col\">\n        {imageUrl && (\n          <div className=\"overflow-hidden\">\n             <img\n              src={imageUrl}\n              alt={title}\n              className=\"w-full h-48 object-cover group-hover:scale-105 transition-transform duration-300\"\n            />\n          </div>\n        )}\n        <div className=\"p-6 flex-grow flex flex-col\">\n          {!imageUrl && <Layers className=\"w-8 h-8 text-blue-500 mb-4\" />}\n          <h3 className=\"text-lg font-bold text-gray-900 group-hover:text-blue-700 transition-colors flex-1\">{title}</h3>\n          <p className=\"text-gray-600 text-sm mt-2 mb-4 line-clamp-3\">{excerpt}</p>\n          <div className=\"mt-auto\">\n            <span className=\"text-sm font-semibold text-blue-600 group-hover:underline flex items-center\">\n              View Details <ArrowRight className=\"w-4 h-4 ml-1 transition-transform group-hover:translate-x-1\" />\n            </span>\n          </div>\n        </div>\n      </div>\n    </Link>\n  );\n};\nexport default ProjectCardComponent;\n",
      "folder": "app/components",
      "nome_file": "ProjectCardComponent.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nconst MarkdownRendererComponent = ({ content }) => {\n  const paragraphs = content ? content.split('\\n').filter(p => p.trim() !== '') : [];\n\n  return (\n    <div className=\"prose prose-blue max-w-none lg:prose-lg text-gray-700 space-y-4\">\n      {paragraphs.map((p, index) => (\n        <p key={index}>{p}</p>\n      ))}\n    </div>\n  );\n};\n\nexport default MarkdownRendererComponent;\n",
      "folder": "app/components",
      "nome_file": "MarkdownRendererComponent.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect } from 'react';\nimport Link from 'next/link';\nimport PublicLayoutComponent from '../components/PublicLayoutComponent';\nimport ArticleCardComponent from '../components/ArticleCardComponent';\nimport ProjectCardComponent from '../components/ProjectCardComponent';\nimport { ArrowRight, Rss, Layers } from 'lucide-react';\n\nconst HomePage = () => {\n  const [articles, setArticles] = useState([]);\n  const [projects, setProjects] = useState([]);\n  const [loading, setLoading] = useState(true);\n\n  useEffect(() => {\n    const fetchData = async () => {\n      try {\n        setLoading(true);\n        const [articlesRes, projectsRes] = await Promise.all([\n          fetch('http://localhost:5000/api/articles?per_page=3'),\n          fetch('http://localhost:5000/api/projects?per_page=3'),\n        ]);\n        const articlesData = await articlesRes.json();\n        const projectsData = await projectsRes.json();\n        setArticles(articlesData.articles || []);\n        setProjects(projectsData.projects || []);\n      } catch (error) {\n        console.error(\"Failed to fetch homepage data:\", error);\n      } finally {\n        setLoading(false);\n      }\n    };\n    fetchData();\n  }, []);\n\n  return (\n    <PublicLayoutComponent>\n      <section className=\"text-center py-16 md:py-24 bg-white rounded-xl shadow-sm border border-gray-100\">\n        <div className=\"max-w-3xl mx-auto px-4\">\n          <p className=\"text-blue-600 font-semibold mb-2\">Machine Learning Engineer & Developer</p>\n          <h1 className=\"text-4xl md:text-5xl font-extrabold text-gray-900 tracking-tight\">\n            Welcome, I'm [Your Name]\n          </h1>\n          <p className=\"mt-6 text-lg text-gray-600 max-w-2xl mx-auto\">\n            I specialize in building intelligent systems and elegant web applications. Here you'll find my thoughts on technology, my latest projects, and a bit about my professional journey.\n          </p>\n          <div className=\"mt-8 flex flex-col sm:flex-row justify-center gap-4\">\n            <Link href=\"/projects\" className=\"inline-flex items-center justify-center px-5 py-3 border border-transparent text-base font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700\">\n              View My Work\n            </Link>\n            <Link href=\"/about\" className=\"inline-flex items-center justify-center px-5 py-3 border border-gray-300 text-base font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50\">\n              About Me\n            </Link>\n          </div>\n        </div>\n      </section>\n\n      <section className=\"py-16 md:py-20\">\n        <div className=\"text-center mb-12\">\n            <h2 className=\"text-3xl font-bold text-gray-900 flex items-center justify-center gap-3\"><Rss className=\"w-7 h-7 text-blue-500\" /> Recent Articles</h2>\n            <p className=\"text-gray-600 mt-2\">My latest thoughts and findings.</p>\n        </div>\n        {loading ? (\n          <p className=\"text-center text-gray-500\">Loading articles...</p>\n        ) : articles.length > 0 ? (\n          <div className=\"grid md:grid-cols-2 lg:grid-cols-3 gap-8\">\n            {articles.map(article => (\n              <ArticleCardComponent key={article.id} article={article} />\n            ))}\n          </div>\n        ) : (\n          <p className=\"text-center text-gray-500\">No articles available yet.</p>\n        )}\n        <div className=\"text-center mt-12\">\n            <Link href=\"/articles\" className=\"font-semibold text-blue-600 hover:text-blue-800 transition-colors flex items-center justify-center gap-2\">\n                View All Articles <ArrowRight className=\"w-4 h-4\" />\n            </Link>\n        </div>\n      </section>\n\n      <section className=\"py-16 md:py-20 bg-white rounded-xl shadow-sm border border-gray-100\">\n         <div className=\"text-center mb-12\">\n            <h2 className=\"text-3xl font-bold text-gray-900 flex items-center justify-center gap-3\"><Layers className=\"w-7 h-7 text-blue-500\" /> Featured Projects</h2>\n            <p className=\"text-gray-600 mt-2\">A selection of my recent work.</p>\n        </div>\n        {loading ? (\n          <p className=\"text-center text-gray-500\">Loading projects...</p>\n        ) : projects.length > 0 ? (\n          <div className=\"grid md:grid-cols-2 lg:grid-cols-3 gap-8\">\n            {projects.map(project => (\n              <ProjectCardComponent key={project.id} project={project} />\n            ))}\n          </div>\n        ) : (\n          <p className=\"text-center text-gray-500\">No projects to display.</p>\n        )}\n        <div className=\"text-center mt-12\">\n            <Link href=\"/projects\" className=\"font-semibold text-blue-600 hover:text-blue-800 transition-colors flex items-center justify-center gap-2\">\n                View All Projects <ArrowRight className=\"w-4 h-4\" />\n            </Link>\n        </div>\n      </section>\n    </PublicLayoutComponent>\n  );\n};\n\nexport default HomePage;\n",
      "folder": "app",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect } from 'react';\nimport PublicLayoutComponent from '../../components/PublicLayoutComponent';\nimport MarkdownRendererComponent from '../../components/MarkdownRendererComponent';\nimport { UserCircle, Loader2 } from 'lucide-react';\n\nconst AboutPage = () => {\n    const [content, setContent] = useState('');\n    const [loading, setLoading] = useState(true);\n    const [error, setError] = useState(null);\n\n    useEffect(() => {\n        const fetchContent = async () => {\n            try {\n                setLoading(true);\n                setError(null);\n                const response = await fetch('http://localhost:5000/api/site-content/about-me');\n                if (!response.ok) {\n                    throw new Error('Failed to fetch content');\n                }\n                const data = await response.json();\n                setContent(data.content);\n            } catch (err) {\n                setError(err.message);\n                console.error(err);\n            } finally {\n                setLoading(false);\n            }\n        };\n\n        fetchContent();\n    }, []);\n\n    return (\n        <PublicLayoutComponent>\n            <div className=\"bg-white p-8 md:p-12 rounded-xl shadow-sm border border-gray-100\">\n                <div className=\"max-w-4xl mx-auto\">\n                    <div className=\"flex flex-col items-center text-center\">\n                        <UserCircle className=\"w-20 h-20 text-blue-500 mb-4\"/>\n                        <h1 className=\"text-4xl font-extrabold text-gray-900 tracking-tight\">About Me</h1>\n                        <p className=\"mt-4 text-lg text-gray-600\">My professional journey, skills, and aspirations.</p>\n                    </div>\n\n                    <hr className=\"my-10 border-gray-200\" />\n                    \n                    <div className=\"w-full\">\n                        {loading && (\n                           <div className=\"flex justify-center items-center h-40\">\n                                <Loader2 className=\"h-10 w-10 animate-spin text-blue-600\" />\n                            </div>\n                        )}\n                        {error && <p className=\"text-center text-red-500\">Error: {error}</p>}\n                        {content && !loading && <MarkdownRendererComponent content={content} />}\n                    </div>\n                </div>\n            </div>\n        </PublicLayoutComponent>\n    );\n};\n\nexport default AboutPage;\n",
      "folder": "app/about",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\nimport React, { useState, useEffect } from 'react';\nimport PublicLayoutComponent from '../../components/PublicLayoutComponent';\nimport ProjectCardComponent from '../../components/ProjectCardComponent';\nimport { Layers, Loader2 } from 'lucide-react';\n\nconst ProjectsPage = () => {\n    const [projects, setProjects] = useState([]);\n    const [loading, setLoading] = useState(true);\n\n    useEffect(() => {\n        const fetchProjects = async () => {\n            try {\n                setLoading(true);\n                const response = await fetch('http://localhost:5000/api/projects');\n                const data = await response.json();\n                setProjects(data.projects || []);\n            } catch (error) {\n                console.error(\"Failed to fetch projects:\", error);\n            } finally {\n                setLoading(false);\n            }\n        };\n        fetchProjects();\n    }, []);\n\n    return (\n        <PublicLayoutComponent>\n            <div className=\"text-center mb-12\">\n                <h1 className=\"text-4xl font-extrabold text-gray-900 tracking-tight flex items-center justify-center gap-3\">\n                    <Layers className=\"w-10 h-10 text-blue-600\" />\n                    Projects\n                </h1>\n                <p className=\"mt-4 text-lg text-gray-600\">A showcase of my work and creations.</p>\n            </div>\n             {loading ? (\n                <div className=\"text-center py-10\">\n                    <Loader2 className=\"h-10 w-10 animate-spin text-blue-600 mx-auto\" />\n                </div>\n            ) : projects.length > 0 ? (\n                <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-8\">\n                    {projects.map(project => (\n                        <ProjectCardComponent key={project.id} project={project} />\n                    ))}\n                </div>\n            ) : (\n                <div className=\"text-center py-16 bg-white rounded-lg shadow-sm border\">\n                    <Layers className=\"w-12 h-12 mx-auto text-gray-400\"/>\n                    <h3 className=\"mt-4 text-lg font-medium text-gray-900\">No projects found.</h3>\n                    <p className=\"mt-1 text-sm text-gray-500\">I'm currently working on new things. Please check back later!</p>\n                </div>\n            )}\n        </PublicLayoutComponent>\n    )\n}\n\nexport default ProjectsPage;\n",
      "folder": "app/projects",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect } from 'react';\nimport { useParams } from 'next/navigation';\nimport PublicLayoutComponent from '../../../components/PublicLayoutComponent';\nimport MarkdownRendererComponent from '../../../components/MarkdownRendererComponent';\nimport { Github, ExternalLink, Loader2, AlertTriangle } from 'lucide-react';\n\nconst ProjectDetailPage = () => {\n    const params = useParams();\n    const { slug } = params;\n    const [project, setProject] = useState(null);\n    const [loading, setLoading] = useState(true);\n    const [error, setError] = useState(null);\n\n    useEffect(() => {\n        if (!slug) return;\n        const fetchProject = async () => {\n            try {\n                setLoading(true);\n                setError(null);\n                const response = await fetch(`http://localhost:5000/api/projects/${slug}`);\n                if (response.status === 404) {\n                    throw new Error('Project not found');\n                }\n                if (!response.ok) {\n                    throw new Error('Failed to fetch project data');\n                }\n                const data = await response.json();\n                setProject(data);\n            } catch (err) {\n                setError(err.message);\n                console.error(err);\n            } finally {\n                setLoading(false);\n            }\n        };\n        fetchProject();\n    }, [slug]);\n\n    if (loading) {\n        return (\n            <PublicLayoutComponent>\n                <div className=\"flex justify-center items-center h-64\">\n                    <Loader2 className=\"h-12 w-12 animate-spin text-blue-600\" />\n                </div>\n            </PublicLayoutComponent>\n        );\n    }\n    \n    if (error) {\n         return (\n            <PublicLayoutComponent>\n                <div className=\"text-center py-16 bg-red-50 rounded-lg border border-red-200\">\n                    <AlertTriangle className=\"w-12 h-12 mx-auto text-red-500\"/>\n                    <h2 className=\"text-2xl font-bold text-red-700 mt-4\">Error</h2>\n                    <p className=\"mt-2 text-red-600\">{error}</p>\n                </div>\n            </PublicLayoutComponent>\n        );\n    }\n\n    if (!project) return null;\n\n    return (\n        <PublicLayoutComponent>\n            <div className=\"bg-white rounded-xl shadow-sm border border-gray-100 overflow-hidden\">\n                <div className=\"p-8 md:p-12\">\n                    <h1 className=\"text-4xl md:text-5xl font-extrabold text-gray-900 tracking-tight\">{project.title}</h1>\n                    <p className=\"mt-2 text-sm text-gray-500\">\n                        Created on {new Date(project.created_at).toLocaleDateString('en-US', { year: 'numeric', month: 'long', day: 'numeric' })}\n                    </p>\n                </div>\n\n                {project.images && project.images.length > 0 && (\n                     <div className=\"grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-2 px-4 pb-4 bg-gray-50\">\n                        {project.images.map(image => (\n                            <div key={image.id} className=\"overflow-hidden rounded-lg aspect-w-16 aspect-h-9\">\n                                <img \n                                    src={`http://localhost:5000${image.url}`} \n                                    alt={image.alt_text || project.title} \n                                    className=\"w-full h-full object-cover transition-transform duration-300 hover:scale-105\"\n                                />\n                            </div>\n                        ))}\n                    </div>\n                )}\n               \n                <div className=\"p-8 md:p-12\">\n                    {project.links && project.links.length > 0 && (\n                        <div className=\"mb-10 flex flex-wrap gap-4\">\n                            {project.links.map(link => (\n                                <a \n                                    key={link.id} \n                                    href={link.url} \n                                    target=\"_blank\" \n                                    rel=\"noopener noreferrer\"\n                                    className=\"inline-flex items-center gap-2 px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-gray-800 hover:bg-gray-900 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-gray-500 transition-colors\"\n                                >\n                                    {link.link_text.toLowerCase().includes('github') ? <Github className=\"w-4 h-4\"/> : <ExternalLink className=\"w-4 h-4\"/>}\n                                    {link.link_text}\n                                </a>\n                            ))}\n                        </div>\n                    )}\n                    \n                    <div className=\"mt-8\">\n                         <h2 className=\"text-2xl font-bold text-gray-800 mb-4\">About this project</h2>\n                        <MarkdownRendererComponent content={project.description} />\n                    </div>\n                </div>\n            </div>\n        </PublicLayoutComponent>\n    );\n};\n\nexport default ProjectDetailPage;\n",
      "folder": "app/projects/[slug]",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\nimport React, { useState, useEffect } from 'react';\nimport PublicLayoutComponent from '../../components/PublicLayoutComponent';\nimport ArticleCardComponent from '../../components/ArticleCardComponent';\nimport { Rss, BookOpen, Loader2 } from 'lucide-react';\n\nconst ArticlesPage = () => {\n    const [articles, setArticles] = useState([]);\n    const [loading, setLoading] = useState(true);\n\n    useEffect(() => {\n        const fetchArticles = async () => {\n            try {\n                setLoading(true);\n                const response = await fetch('http://localhost:5000/api/articles');\n                const data = await response.json();\n                setArticles(data.articles || []);\n            } catch (error) {\n                console.error(\"Failed to fetch articles:\", error);\n            } finally {\n                setLoading(false);\n            }\n        };\n        fetchArticles();\n    }, []);\n\n    return (\n        <PublicLayoutComponent>\n             <div className=\"text-center mb-12\">\n                <h1 className=\"text-4xl font-extrabold text-gray-900 tracking-tight flex items-center justify-center gap-3\">\n                    <Rss className=\"w-10 h-10 text-blue-600\" />\n                    Articles\n                </h1>\n                <p className=\"mt-4 text-lg text-gray-600\">Insights, tutorials, and stories from my work.</p>\n            </div>\n             {loading ? (\n                <div className=\"text-center py-10\">\n                     <Loader2 className=\"h-10 w-10 animate-spin text-blue-600 mx-auto\" />\n                </div>\n            ) : articles.length > 0 ? (\n                <div className=\"space-y-8 max-w-4xl mx-auto\">\n                    {articles.map(article => (\n                        <ArticleCardComponent key={article.id} article={article} />\n                    ))}\n                </div>\n            ) : (\n                <div className=\"text-center py-16 bg-white rounded-lg shadow-sm border\">\n                    <BookOpen className=\"w-12 h-12 mx-auto text-gray-400\"/>\n                    <h3 className=\"mt-4 text-lg font-medium text-gray-900\">No articles available.</h3>\n                    <p className=\"mt-1 text-sm text-gray-500\">I'm currently writing new content. Stay tuned!</p>\n                </div>\n            )}\n        </PublicLayoutComponent>\n    )\n}\n\nexport default ArticlesPage;\n",
      "folder": "app/articles",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect } from 'react';\nimport { useParams, useRouter } from 'next/navigation';\nimport PublicLayoutComponent from '../../components/PublicLayoutComponent';\nimport MarkdownRendererComponent from '../../components/MarkdownRendererComponent';\nimport { Loader2, AlertTriangle, Calendar, Tag as TagIcon, ArrowLeft } from 'lucide-react';\nimport Link from 'next/link';\n\nconst ArticleDetailPage = () => {\n    const params = useParams();\n    const router = useRouter();\n    const { slug } = params;\n    const [article, setArticle] = useState(null);\n    const [loading, setLoading] = useState(true);\n    const [error, setError] = useState(null);\n\n    useEffect(() => {\n        if (!slug) return;\n        const fetchArticle = async () => {\n            try {\n                setLoading(true);\n                setError(null);\n                const response = await fetch(`http://localhost:5000/api/articles/${slug}`);\n                if (response.status === 404) {\n                    throw new Error('Article not found.');\n                }\n                if (!response.ok) {\n                    throw new Error('Failed to fetch the article.');\n                }\n                const data = await response.json();\n                setArticle(data);\n            } catch (err) {\n                setError(err.message);\n                console.error(err);\n            } finally {\n                setLoading(false);\n            }\n        };\n        fetchArticle();\n    }, [slug]);\n\n    if (loading) {\n        return (\n            <PublicLayoutComponent>\n                <div className=\"flex justify-center items-center h-64\">\n                    <Loader2 className=\"h-12 w-12 animate-spin text-blue-600\" />\n                </div>\n            </PublicLayoutComponent>\n        );\n    }\n\n    if (error) {\n        return (\n            <PublicLayoutComponent>\n                <div className=\"text-center py-16 bg-red-50 rounded-lg border border-red-200\">\n                    <AlertTriangle className=\"w-12 h-12 mx-auto text-red-500\" />\n                    <h2 className=\"text-2xl font-bold text-red-700 mt-4\">An Error Occurred</h2>\n                    <p className=\"mt-2 text-red-600\">{error}</p>\n                    <button onClick={() => router.push('/articles')} className=\"mt-6 inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md shadow-sm text-white bg-blue-600 hover:bg-blue-700\">\n                        Back to Articles\n                    </button>\n                </div>\n            </PublicLayoutComponent>\n        );\n    }\n\n    if (!article) return null;\n\n    const formattedDate = new Date(article.created_at).toLocaleDateString('en-US', {\n        year: 'numeric',\n        month: 'long',\n        day: 'numeric',\n    });\n\n    return (\n        <PublicLayoutComponent>\n            <div className=\"max-w-4xl mx-auto\">\n                 <div className=\"mb-8\">\n                    <Link href=\"/articles\" className=\"inline-flex items-center text-sm font-medium text-gray-600 hover:text-blue-600 transition-colors\">\n                        <ArrowLeft className=\"w-4 h-4 mr-2\" />\n                        Back to all articles\n                    </Link>\n                </div>\n                <div className=\"bg-white p-8 md:p-12 rounded-xl shadow-sm border border-gray-100\">\n                    <article>\n                        <header className=\"mb-8 text-center\">\n                             <div className=\"flex flex-wrap justify-center gap-2 mb-4\">\n                                {article.tags.map(tag => (\n                                    <span key={tag.slug} className=\"text-xs font-semibold text-blue-700 bg-blue-100 rounded-full px-3 py-1 flex items-center gap-1\">\n                                        <TagIcon className=\"w-3 h-3\" /> {tag.name}\n                                    </span>\n                                ))}\n                            </div>\n                            <h1 className=\"text-4xl md:text-5xl font-extrabold text-gray-900 tracking-tight\">{article.title}</h1>\n                            <div className=\"mt-4 flex items-center justify-center text-sm text-gray-500\">\n                                <Calendar className=\"w-4 h-4 mr-2\" />\n                                <span>Published on {formattedDate}</span>\n                            </div>\n                        </header>\n                        \n                        <div className=\"mt-12\">\n                            <MarkdownRendererComponent content={article.content_markdown} />\n                        </div>\n                    </article>\n                </div>\n            </div>\n        </PublicLayoutComponent>\n    );\n};\n\nexport default ArticleDetailPage;\n",
      "folder": "app/articles/[slug]",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState } from 'react';\nimport { useRouter } from 'next/navigation';\nimport { Bot, LogIn, AlertCircle, Loader2 } from 'lucide-react';\n\nconst AdminLoginPage = () => {\n    const router = useRouter();\n    const [username, setUsername] = useState('');\n    const [password, setPassword] = useState('');\n    const [error, setError] = useState('');\n    const [loading, setLoading] = useState(false);\n\n    const handleLogin = async (e) => {\n        e.preventDefault();\n        setError('');\n        setLoading(true);\n\n        try {\n            const response = await fetch('http://localhost:5000/api/auth/login', {\n                method: 'POST',\n                headers: { 'Content-Type': 'application/json' },\n                body: JSON.stringify({ username, password }),\n            });\n\n            if (!response.ok) {\n                const errorData = await response.json();\n                throw new Error(errorData.message || 'Invalid credentials');\n            }\n\n            const data = await response.json();\n            if (typeof window !== 'undefined') {\n              localStorage.setItem('jwt_token', data.access_token);\n            }\n            router.push('/admin/dashboard');\n        } catch (err) {\n            setError(err.message);\n        } finally {\n            setLoading(false);\n        }\n    };\n\n    return (\n        <div className=\"min-h-screen flex items-center justify-center bg-gray-100 p-4\">\n            <div className=\"w-full max-w-md\">\n              <div className=\"text-center mb-6\">\n                <Bot className=\"mx-auto h-12 w-auto text-blue-600\" />\n                <h2 className=\"mt-4 text-2xl font-bold text-gray-800\">Administrator Access</h2>\n              </div>\n            \n              <div className=\"bg-white p-8 rounded-xl shadow-lg border border-gray-200\">\n                <form onSubmit={handleLogin} className=\"space-y-6\">\n                    <div>\n                        <label htmlFor=\"username\" className=\"block text-sm font-medium text-gray-700\">Username</label>\n                        <div className=\"mt-1\">\n                            <input\n                                id=\"username\"\n                                name=\"username\"\n                                type=\"text\"\n                                value={username}\n                                onChange={(e) => setUsername(e.target.value)}\n                                required\n                                className=\"w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm\"\n                            />\n                        </div>\n                    </div>\n\n                    <div>\n                        <label htmlFor=\"password\" className=\"block text-sm font-medium text-gray-700\">Password</label>\n                        <div className=\"mt-1\">\n                             <input\n                                id=\"password\"\n                                name=\"password\"\n                                type=\"password\"\n                                value={password}\n                                onChange={(e) => setPassword(e.target.value)}\n                                required\n                                className=\"w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm\"\n                            />\n                        </div>\n                    </div>\n\n                    {error && (\n                        <div className=\"flex items-center p-3 text-sm text-red-700 bg-red-100 rounded-md border border-red-200\">\n                            <AlertCircle className=\"w-5 h-5 mr-2 flex-shrink-0\" />\n                            <span>{error}</span>\n                        </div>\n                    )}\n\n                    <div>\n                        <button\n                            type=\"submit\"\n                            disabled={loading}\n                            className=\"w-full flex justify-center items-center py-2.5 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500 disabled:bg-blue-300 disabled:cursor-not-allowed\"\n                        >\n                            {loading ? <Loader2 className=\"h-5 w-5 animate-spin\" /> : <LogIn className=\"h-5 w-5 mr-2\"/>}\n                            {loading ? 'Signing in...' : 'Sign In'}\n                        </button>\n                    </div>\n                </form>\n              </div>\n            </div>\n        </div>\n    );\n};\n\nexport default AdminLoginPage;\n",
      "folder": "app/admin/login",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect } from 'react';\nimport Link from 'next/link';\nimport { LayoutDashboard, FileText, Layers, Settings, LogOut, Loader2, ArrowRight } from 'lucide-react';\nimport { useRouter, usePathname } from 'next/navigation';\n\n// AdminLayout has to be defined within each page that uses it\nconst AdminLayout = ({ children, title }) => {\n    const router = useRouter();\n    const pathname = usePathname();\n\n    useEffect(() => {\n        const token = localStorage.getItem('jwt_token');\n        if (!token) {\n            router.replace('/admin/login');\n        }\n    }, [router]);\n\n    const handleLogout = () => {\n        localStorage.removeItem('jwt_token');\n        router.push('/admin/login');\n    };\n\n    const navItems = [\n        { href: '/admin/dashboard', icon: LayoutDashboard, label: 'Dashboard' },\n        { href: '/admin/articles', icon: FileText, label: 'Articles' },\n        { href: '/admin/projects', icon: Layers, label: 'Projects' },\n        { href: '/admin/settings', icon: Settings, label: 'Settings' },\n    ];\n\n    return (\n        <div className=\"flex min-h-screen bg-gray-50\">\n            <aside className=\"w-64 flex-shrink-0 bg-white border-r border-gray-200 flex flex-col\">\n                <div className=\"h-16 flex items-center px-4 border-b border-gray-200\">\n                    <h1 className=\"text-xl font-bold text-gray-800\">Admin Panel</h1>\n                </div>\n                <nav className=\"flex-1 p-4 space-y-2\">\n                    {navItems.map(item => (\n                        <Link key={item.href} href={item.href} className={`flex items-center px-3 py-2.5 text-sm font-medium rounded-md transition-colors ${pathname === item.href ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100 hover:text-gray-900'}`}>\n                            <item.icon className=\"w-5 h-5 mr-3\" />\n                            <span>{item.label}</span>\n                        </Link>\n                    ))}\n                </nav>\n                <div className=\"p-4 border-t border-gray-200\">\n                     <button onClick={handleLogout} className=\"flex w-full items-center px-3 py-2.5 text-sm font-medium rounded-md text-red-600 hover:bg-red-50\">\n                        <LogOut className=\"w-5 h-5 mr-3\" />\n                        <span>Logout</span>\n                    </button>\n                </div>\n            </aside>\n            <div className=\"flex-1 flex flex-col\">\n                <header className=\"h-16 bg-white border-b border-gray-200 flex items-center px-6\">\n                    <h2 className=\"text-2xl font-semibold text-gray-800\">{title}</h2>\n                </header>\n                <main className=\"flex-1 p-6\">\n                    {children}\n                </main>\n            </div>\n        </div>\n    );\n};\n\nconst StatCard = ({ title, value, icon, link }) => (\n    <div className=\"bg-white p-6 rounded-lg shadow-sm border border-gray-100\">\n        <div className=\"flex items-start justify-between\">\n            <div>\n              <p className=\"text-sm font-medium text-gray-500\">{title}</p>\n              <p className=\"text-3xl font-bold text-gray-900 mt-1\">{value}</p>\n            </div>\n            <div className=\"bg-blue-100 text-blue-600 p-3 rounded-full\">{icon}</div>\n        </div>\n        <div className=\"mt-4\">\n            <Link href={link} className=\"text-sm font-semibold text-blue-600 hover:underline flex items-center\">View All <ArrowRight className=\"w-4 h-4 ml-1\" /></Link>\n        </div>\n    </div>\n);\n\n\nconst AdminDashboardPage = () => {\n    const [stats, setStats] = useState({ articles: 0, projects: 0 });\n    const [loading, setLoading] = useState(true);\n\n    useEffect(() => {\n        const fetchStats = async () => {\n            try {\n                const token = localStorage.getItem('jwt_token');\n                const headers = {\n                    'Authorization': `Bearer ${token}`,\n                    'Content-Type': 'application/json'\n                };\n\n                const [articlesRes, projectsRes] = await Promise.all([\n                    fetch('http://localhost:5000/api/articles', { headers }),\n                    fetch('http://localhost:5000/api/projects', { headers }),\n                ]);\n\n                const articlesData = await articlesRes.json();\n                const projectsData = await projectsRes.json();\n\n                setStats({ articles: articlesData.total || 0, projects: projectsData.total || 0 });\n\n            } catch (error) {\n                console.error(\"Failed to fetch stats:\", error);\n            } finally {\n                setLoading(false);\n            }\n        };\n        fetchStats();\n    }, []);\n\n    return (\n        <AdminLayout title=\"Dashboard\">\n            {loading ? (\n                <div className=\"flex justify-center items-center h-full\">\n                    <Loader2 className=\"w-8 h-8 animate-spin text-blue-500\" />\n                </div>\n            ) : (\n                <div className=\"grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6\">\n                    <StatCard title=\"Total Articles\" value={stats.articles} icon={<FileText className=\"w-6 h-6\" />} link=\"/admin/articles\" />\n                    <StatCard title=\"Total Projects\" value={stats.projects} icon={<Layers className=\"w-6 h-6\" />} link=\"/admin/projects\" />\n                </div>\n            )}\n        </AdminLayout>\n    );\n};\n\nexport default AdminDashboardPage;\n",
      "folder": "app/admin/dashboard",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect, useCallback } from 'react';\nimport Link from 'next/link';\nimport { LayoutDashboard, FileText, Layers, Settings, LogOut, PlusCircle, Edit, Trash2, Loader2, AlertCircle } from 'lucide-react';\nimport { useRouter, usePathname } from 'next/navigation';\n\n// AdminLayout has to be defined within each page that uses it\nconst AdminLayout = ({ children, title }) => {\n    const router = useRouter();\n    const pathname = usePathname();\n    useEffect(() => {\n        const token = localStorage.getItem('jwt_token');\n        if (!token) {\n            router.replace('/admin/login');\n        }\n    }, [router]);\n    const handleLogout = () => { localStorage.removeItem('jwt_token'); router.push('/admin/login'); };\n    const navItems = [\n        { href: '/admin/dashboard', icon: LayoutDashboard, label: 'Dashboard' },\n        { href: '/admin/articles', icon: FileText, label: 'Articles' },\n        { href: '/admin/projects', icon: Layers, label: 'Projects' },\n        { href: '/admin/settings', icon: Settings, label: 'Settings' },\n    ];\n    return (\n        <div className=\"flex min-h-screen bg-gray-50\">\n            <aside className=\"w-64 flex-shrink-0 bg-white border-r border-gray-200 flex flex-col\">\n                <div className=\"h-16 flex items-center px-4 border-b border-gray-200\"><h1 className=\"text-xl font-bold text-gray-800\">Admin Panel</h1></div>\n                <nav className=\"flex-1 p-4 space-y-2\">\n                    {navItems.map(item => (\n                        <Link key={item.href} href={item.href} className={`flex items-center px-3 py-2.5 text-sm font-medium rounded-md transition-colors ${pathname === item.href ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100 hover:text-gray-900'}`}>\n                            <item.icon className=\"w-5 h-5 mr-3\" /><span>{item.label}</span>\n                        </Link>\n                    ))}\n                </nav>\n                <div className=\"p-4 border-t border-gray-200\">\n                     <button onClick={handleLogout} className=\"flex w-full items-center px-3 py-2.5 text-sm font-medium rounded-md text-red-600 hover:bg-red-50\"><LogOut className=\"w-5 h-5 mr-3\" /><span>Logout</span></button>\n                </div>\n            </aside>\n            <div className=\"flex-1 flex flex-col\">\n                <header className=\"h-16 bg-white border-b border-gray-200 flex items-center px-6 justify-between\">{children[0]}</header>\n                <main className=\"flex-1 p-6\">{children[1]}</main>\n            </div>\n        </div>\n    );\n};\n\nconst AdminProjectsPage = () => {\n    const [projects, setProjects] = useState([]);\n    const [loading, setLoading] = useState(true);\n    const [error, setError] = useState('');\n\n    const fetchProjects = useCallback(async () => {\n        try {\n            setLoading(true);\n            const response = await fetch('http://localhost:5000/api/projects');\n            if (!response.ok) throw new Error('Failed to fetch projects');\n            const data = await response.json();\n            setProjects(data.projects || []);\n        } catch (err) {\n            setError(err.message);\n        } finally {\n            setLoading(false);\n        }\n    }, []);\n\n    useEffect(() => {\n        fetchProjects();\n    }, [fetchProjects]);\n\n    const handleDelete = async (id) => {\n        if (!confirm('Are you sure you want to delete this project?')) return;\n\n        const token = localStorage.getItem('jwt_token');\n        try {\n            const response = await fetch(`http://localhost:5000/api/admin/projects/${id}`, {\n                method: 'DELETE',\n                headers: { 'Authorization': `Bearer ${token}` },\n            });\n            if (!response.ok) throw new Error('Failed to delete project');\n            fetchProjects(); // Refresh list\n        } catch (err) {\n            setError(err.message);\n        }\n    };\n\n    // Modal and form logic would be here. Due to complexity, this example focuses on listing and deleting.\n    // A full implementation would use a <Dialog> from shadcn/ui for create/edit forms.\n\n    return (\n        <AdminLayout>\n            <>\n                <h2 className=\"text-2xl font-semibold text-gray-800\">Manage Projects</h2>\n                <button className=\"inline-flex items-center justify-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700\">\n                    <PlusCircle className=\"w-5 h-5 mr-2\" />\n                    Create New Project\n                </button>\n            </>\n            <div className=\"bg-white rounded-lg shadow-sm border border-gray-200\">\n                {loading && <div className=\"p-6 text-center\"><Loader2 className=\"w-6 h-6 animate-spin mx-auto text-blue-500\" /></div>}\n                {error && <div className=\"p-4 m-4 bg-red-100 text-red-700 rounded-md flex items-center\"><AlertCircle className=\"w-5 h-5 mr-2\"/>{error}</div>}\n                {!loading && !error && (\n                    <table className=\"w-full text-sm text-left text-gray-600\">\n                        <thead className=\"text-xs text-gray-700 uppercase bg-gray-50 border-b\">\n                            <tr>\n                                <th scope=\"col\" className=\"px-6 py-3\">Title</th>\n                                <th scope=\"col\" className=\"px-6 py-3\">Date Created</th>\n                                <th scope=\"col\" className=\"px-6 py-3 text-right\">Actions</th>\n                            </tr>\n                        </thead>\n                        <tbody>\n                            {projects.map(project => (\n                                <tr key={project.id} className=\"bg-white border-b hover:bg-gray-50\">\n                                    <td className=\"px-6 py-4 font-medium text-gray-900 whitespace-nowrap\">{project.title}</td>\n                                    <td className=\"px-6 py-4\">{new Date(project.created_at).toLocaleDateString()}</td>\n                                    <td className=\"px-6 py-4 text-right space-x-2\">\n                                        <button className=\"font-medium text-blue-600 hover:underline p-1\"><Edit className=\"w-4 h-4\" /></button>\n                                        <button onClick={() => handleDelete(project.id)} className=\"font-medium text-red-600 hover:underline p-1\"><Trash2 className=\"w-4 h-4\" /></button>\n                                    </td>\n                                </tr>\n                            ))}\n                        </tbody>\n                    </table>\n                )}\n                {!loading && projects.length === 0 && <div className=\"p-6 text-center text-gray-500\">No projects found.</div>}\n            </div>\n        </AdminLayout>\n    );\n};\n\nexport default AdminProjectsPage;\n",
      "folder": "app/admin/projects",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect, useCallback } from 'react';\nimport Link from 'next/link';\nimport { LayoutDashboard, FileText, Layers, Settings, LogOut, PlusCircle, Edit, Trash2, Loader2, AlertCircle } from 'lucide-react';\nimport { useRouter, usePathname } from 'next/navigation';\n\n// AdminLayout has to be defined within each page that uses it\nconst AdminLayout = ({ children, title }) => {\n    const router = useRouter();\n    const pathname = usePathname();\n    useEffect(() => {\n        const token = localStorage.getItem('jwt_token');\n        if (!token) {\n            router.replace('/admin/login');\n        }\n    }, [router]);\n    const handleLogout = () => { localStorage.removeItem('jwt_token'); router.push('/admin/login'); };\n    const navItems = [\n        { href: '/admin/dashboard', icon: LayoutDashboard, label: 'Dashboard' },\n        { href: '/admin/articles', icon: FileText, label: 'Articles' },\n        { href: '/admin/projects', icon: Layers, label: 'Projects' },\n        { href: '/admin/settings', icon: Settings, label: 'Settings' },\n    ];\n    return (\n        <div className=\"flex min-h-screen bg-gray-50\">\n            <aside className=\"w-64 flex-shrink-0 bg-white border-r border-gray-200 flex flex-col\">\n                <div className=\"h-16 flex items-center px-4 border-b border-gray-200\"><h1 className=\"text-xl font-bold text-gray-800\">Admin Panel</h1></div>\n                <nav className=\"flex-1 p-4 space-y-2\">\n                    {navItems.map(item => (\n                        <Link key={item.href} href={item.href} className={`flex items-center px-3 py-2.5 text-sm font-medium rounded-md transition-colors ${pathname === item.href ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100 hover:text-gray-900'}`}>\n                            <item.icon className=\"w-5 h-5 mr-3\" /><span>{item.label}</span>\n                        </Link>\n                    ))}\n                </nav>\n                <div className=\"p-4 border-t border-gray-200\">\n                     <button onClick={handleLogout} className=\"flex w-full items-center px-3 py-2.5 text-sm font-medium rounded-md text-red-600 hover:bg-red-50\"><LogOut className=\"w-5 h-5 mr-3\" /><span>Logout</span></button>\n                </div>\n            </aside>\n            <div className=\"flex-1 flex flex-col\">\n                <header className=\"h-16 bg-white border-b border-gray-200 flex items-center px-6 justify-between\">{children[0]}</header>\n                <main className=\"flex-1 p-6\">{children[1]}</main>\n            </div>\n        </div>\n    );\n};\n\nconst AdminArticlesPage = () => {\n    const [articles, setArticles] = useState([]);\n    const [loading, setLoading] = useState(true);\n    const [error, setError] = useState('');\n\n    const fetchArticles = useCallback(async () => {\n        try {\n            setLoading(true);\n            const response = await fetch('http://localhost:5000/api/articles');\n            if (!response.ok) throw new Error('Failed to fetch articles');\n            const data = await response.json();\n            setArticles(data.articles || []);\n        } catch (err) {\n            setError(err.message);\n        } finally {\n            setLoading(false);\n        }\n    }, []);\n\n    useEffect(() => {\n        fetchArticles();\n    }, [fetchArticles]);\n\n    const handleDelete = async (id) => {\n        if (!confirm('Are you sure you want to delete this article?')) return;\n\n        const token = localStorage.getItem('jwt_token');\n        try {\n            const response = await fetch(`http://localhost:5000/api/admin/articles/${id}`, {\n                method: 'DELETE',\n                headers: { 'Authorization': `Bearer ${token}` },\n            });\n            if (!response.ok) throw new Error('Failed to delete article');\n            fetchArticles(); // Refresh list\n        } catch (err) {\n            setError(err.message);\n        }\n    };\n\n    return (\n        <AdminLayout>\n            <>\n                <h2 className=\"text-2xl font-semibold text-gray-800\">Manage Articles</h2>\n                <Link href=\"/admin/articles/create\" className=\"inline-flex items-center justify-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700\">\n                    <PlusCircle className=\"w-5 h-5 mr-2\" />\n                    Create New Article\n                </Link>\n            </>\n            <div className=\"bg-white rounded-lg shadow-sm border border-gray-200 overflow-hidden\">\n                 {loading && <div className=\"p-6 text-center\"><Loader2 className=\"w-6 h-6 animate-spin mx-auto text-blue-500\" /></div>}\n                 {error && <div className=\"p-4 m-4 bg-red-100 text-red-700 rounded-md flex items-center\"><AlertCircle className=\"w-5 h-5 mr-2\"/>{error}</div>}\n                {!loading && !error && (\n                    <table className=\"w-full text-sm text-left text-gray-600\">\n                        <thead className=\"text-xs text-gray-700 uppercase bg-gray-50 border-b\">\n                            <tr>\n                                <th scope=\"col\" className=\"px-6 py-3\">Title</th>\n                                <th scope=\"col\" className=\"px-6 py-3\">Date Created</th>\n                                <th scope=\"col\" className=\"px-6 py-3\">Status</th>\n                                <th scope=\"col\" className=\"px-6 py-3 text-right\">Actions</th>\n                            </tr>\n                        </thead>\n                        <tbody>\n                            {articles.map(article => (\n                                <tr key={article.id} className=\"bg-white border-b hover:bg-gray-50\">\n                                    <td className=\"px-6 py-4 font-medium text-gray-900 whitespace-nowrap\">{article.title}</td>\n                                    <td className=\"px-6 py-4\">{new Date(article.created_at).toLocaleDateString()}</td>\n                                    <td className=\"px-6 py-4\"><span className=\"bg-green-100 text-green-800 text-xs font-medium mr-2 px-2.5 py-0.5 rounded-full\">Published</span></td>\n                                    <td className=\"px-6 py-4 text-right space-x-2\">\n                                        <Link href={`/admin/articles/edit/${article.id}`} className=\"font-medium text-blue-600 hover:underline p-1 inline-block\"><Edit className=\"w-4 h-4\" /></Link>\n                                        <button onClick={() => handleDelete(article.id)} className=\"font-medium text-red-600 hover:underline p-1\"><Trash2 className=\"w-4 h-4\" /></button>\n                                    </td>\n                                </tr>\n                            ))}\n                        </tbody>\n                    </table>\n                )}\n                {!loading && articles.length === 0 && <div className=\"p-6 text-center text-gray-500\">No articles found.</div>}\n            </div>\n        </AdminLayout>\n    );\n};\n\nexport default AdminArticlesPage;\n",
      "folder": "app/admin/articles",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect, useCallback } from 'react';\nimport Link from 'next/link';\nimport { LayoutDashboard, FileText, Layers, Settings, LogOut, Loader2, Save, AlertCircle } from 'lucide-react';\nimport { useRouter, usePathname } from 'next/navigation';\n\n// AdminLayout has to be defined within each page that uses it\nconst AdminLayout = ({ children, title }) => {\n    const router = useRouter();\n    const pathname = usePathname();\n    useEffect(() => {\n        const token = localStorage.getItem('jwt_token');\n        if (!token) {\n            router.replace('/admin/login');\n        }\n    }, [router]);\n    const handleLogout = () => { localStorage.removeItem('jwt_token'); router.push('/admin/login'); };\n    const navItems = [\n        { href: '/admin/dashboard', icon: LayoutDashboard, label: 'Dashboard' },\n        { href: '/admin/articles', icon: FileText, label: 'Articles' },\n        { href: '/admin/projects', icon: Layers, label: 'Projects' },\n        { href: '/admin/settings', icon: Settings, label: 'Settings' },\n    ];\n    return (\n        <div className=\"flex min-h-screen bg-gray-50\">\n            <aside className=\"w-64 flex-shrink-0 bg-white border-r border-gray-200 flex flex-col\">\n                <div className=\"h-16 flex items-center px-4 border-b border-gray-200\"><h1 className=\"text-xl font-bold text-gray-800\">Admin Panel</h1></div>\n                <nav className=\"flex-1 p-4 space-y-2\">\n                    {navItems.map(item => (\n                        <Link key={item.href} href={item.href} className={`flex items-center px-3 py-2.5 text-sm font-medium rounded-md transition-colors ${pathname === item.href ? 'bg-blue-100 text-blue-700' : 'text-gray-600 hover:bg-gray-100 hover:text-gray-900'}`}>\n                            <item.icon className=\"w-5 h-5 mr-3\" /><span>{item.label}</span>\n                        </Link>\n                    ))}\n                </nav>\n                <div className=\"p-4 border-t border-gray-200\">\n                     <button onClick={handleLogout} className=\"flex w-full items-center px-3 py-2.5 text-sm font-medium rounded-md text-red-600 hover:bg-red-50\"><LogOut className=\"w-5 h-5 mr-3\" /><span>Logout</span></button>\n                </div>\n            </aside>\n            <div className=\"flex-1 flex flex-col\">\n                <header className=\"h-16 bg-white border-b border-gray-200 flex items-center px-6\">\n                    <h2 className=\"text-2xl font-semibold text-gray-800\">{title}</h2>\n                </header>\n                <main className=\"flex-1 p-6\">\n                    {children}\n                </main>\n            </div>\n        </div>\n    );\n};\n\nconst AdminSettingsPage = () => {\n    const [content, setContent] = useState('');\n    const [loading, setLoading] = useState(true);\n    const [saving, setSaving] = useState(false);\n    const [error, setError] = useState('');\n    const [success, setSuccess] = useState('');\n\n    const fetchContent = useCallback(async () => {\n        try {\n            setLoading(true);\n            const response = await fetch('http://localhost:5000/api/site-content/about-me');\n            if (!response.ok) throw new Error('Failed to load content');\n            const data = await response.json();\n            setContent(data.content);\n        } catch (err) {\n            setError(err.message);\n        } finally {\n            setLoading(false);\n        }\n    }, []);\n\n    useEffect(() => {\n        fetchContent();\n    }, [fetchContent]);\n\n    const handleSave = async (e) => {\n        e.preventDefault();\n        setSaving(true);\n        setError('');\n        setSuccess('');\n        const token = localStorage.getItem('jwt_token');\n\n        try {\n            const response = await fetch(`http://localhost:5000/api/admin/site-content/about-me`, {\n                method: 'PUT',\n                headers: {\n                    'Authorization': `Bearer ${token}`,\n                    'Content-Type': 'application/json',\n                },\n                body: JSON.stringify({ content }),\n            });\n            if (!response.ok) throw new Error('Failed to save content');\n            setSuccess('Content updated successfully!');\n        } catch (err) {\n            setError(err.message);\n        } finally {\n            setSaving(false);\n            setTimeout(() => setSuccess(''), 3000);\n        }\n    };\n\n    return (\n        <AdminLayout title=\"Site Settings\">\n            <div className=\"max-w-3xl mx-auto\">\n                <div className=\"bg-white rounded-lg shadow-sm border border-gray-200\">\n                    <form onSubmit={handleSave}>\n                        <div className=\"p-6\">\n                            <h3 className=\"text-lg font-medium leading-6 text-gray-900\">Update About Me Page</h3>\n                            <div className=\"mt-4\">\n                                {loading ? (\n                                    <div className=\"flex justify-center items-center h-48\">\n                                        <Loader2 className=\"w-8 h-8 animate-spin text-blue-500\" />\n                                    </div>\n                                ) : (\n                                    <textarea\n                                        rows=\"15\"\n                                        value={content}\n                                        onChange={(e) => setContent(e.target.value)}\n                                        className=\"w-full px-3 py-2 border border-gray-300 rounded-md shadow-sm placeholder-gray-400 focus:outline-none focus:ring-blue-500 focus:border-blue-500 sm:text-sm\"\n                                        placeholder=\"Enter the content for the About Me page...\"\n                                    />\n                                )}\n                            </div>\n                        </div>\n                        <div className=\"bg-gray-50 px-6 py-4 flex justify-end items-center space-x-4\">\n                            {error && <p className='text-sm text-red-600 flex items-center'><AlertCircle className='w-4 h-4 mr-1' />{error}</p>}\n                            {success && <p className='text-sm text-green-600'>{success}</p>}\n                            <button\n                                type=\"submit\"\n                                disabled={saving || loading}\n                                className=\"inline-flex items-center justify-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 disabled:bg-blue-300\"\n                            >\n                                {saving ? <Loader2 className=\"w-5 h-5 mr-2 animate-spin\" /> : <Save className=\"w-5 h-5 mr-2\"/>}\n                                {saving ? 'Saving...' : 'Save Changes'}\n                            </button>\n                        </div>\n                    </form>\n                </div>\n            </div>\n        </AdminLayout>\n    );\n};\n\nexport default AdminSettingsPage;\n",
      "folder": "app/admin/settings",
      "nome_file": "page.jsx",
      "categoria": "pagina"
    },
    {
      "codice": "'use client';\nimport * as React from 'react';\nimport { cva } from 'class-variance-authority';\nimport { Loader2 } from 'lucide-react';\n\nconst buttonVariants = cva(\n  'inline-flex items-center justify-center rounded-md text-sm font-medium ring-offset-white transition-colors focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-2 disabled:pointer-events-none disabled:opacity-50',\n  {\n    variants: {\n      variant: {\n        primary: 'bg-blue-600 text-white hover:bg-blue-700',\n        secondary: 'bg-gray-100 text-gray-800 hover:bg-gray-200',\n        danger: 'bg-red-600 text-white hover:bg-red-700',\n        outline: 'border border-gray-300 bg-white hover:bg-gray-50',\n        ghost: 'hover:bg-gray-100 text-gray-800',\n      },\n      size: {\n        default: 'h-10 px-4 py-2',\n        sm: 'h-9 rounded-md px-3',\n        lg: 'h-11 rounded-md px-8',\n      },\n    },\n    defaultVariants: {\n      variant: 'primary',\n      size: 'default',\n    },\n  }\n);\n\nconst ButtonComponent = React.forwardRef(({ className, variant, size, children, disabled, isLoading, ...props }, ref) => {\n  const combinedClassName = [buttonVariants({ variant, size }), className].filter(Boolean).join(' ');\n\n  return (\n    <button\n      className={combinedClassName}\n      ref={ref}\n      disabled={disabled || isLoading}\n      {...props}\n    >\n      {isLoading && <Loader2 className=\"mr-2 h-4 w-4 animate-spin\" />}\n      {children}\n    </button>\n  );\n});\nButtonComponent.displayName = 'Button';\n\nexport default ButtonComponent;\n",
      "folder": "app/components",
      "nome_file": "ButtonComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\nimport * as React from 'react';\n\nconst InputFieldComponent = React.forwardRef(({ className, type, label, id, error, ...props }, ref) => {\n  const inputId = id || React.useId();\n  const baseClasses = 'flex h-10 w-full rounded-md border bg-white px-3 py-2 text-sm text-gray-900 ring-offset-white file:border-0 file:bg-transparent file:text-sm file:font-medium placeholder:text-gray-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-1 disabled:cursor-not-allowed disabled:opacity-50';\n  const borderClasses = error ? 'border-red-500 focus-visible:ring-red-500' : 'border-gray-300';\n  const combinedClassName = [baseClasses, borderClasses, className].filter(Boolean).join(' ');\n  \n  return (\n    <div className=\"w-full\">\n      {label && (\n        <label htmlFor={inputId} className=\"block text-sm font-medium text-gray-700 mb-1.5\">\n          {label}\n        </label>\n      )}\n      <input\n        type={type}\n        id={inputId}\n        className={combinedClassName}\n        ref={ref}\n        {...props}\n      />\n      {error && <p className=\"mt-1.5 text-sm text-red-600\">{error}</p>}\n    </div>\n  );\n});\nInputFieldComponent.displayName = 'InputField';\n\nexport default InputFieldComponent;\n",
      "folder": "app/components",
      "nome_file": "InputFieldComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\nimport * as React from 'react';\n\nconst TextareaFieldComponent = React.forwardRef(({ className, label, id, error, rows = 3, ...props }, ref) => {\n  const textareaId = id || React.useId();\n  const baseClasses = 'flex w-full rounded-md border bg-white px-3 py-2 text-sm text-gray-900 ring-offset-white placeholder:text-gray-500 focus-visible:outline-none focus-visible:ring-2 focus-visible:ring-blue-500 focus-visible:ring-offset-1 disabled:cursor-not-allowed disabled:opacity-50 resize-y';\n  const borderClasses = error ? 'border-red-500 focus-visible:ring-red-500' : 'border-gray-300';\n  const combinedClassName = [baseClasses, borderClasses, className].filter(Boolean).join(' ');\n\n  return (\n    <div className=\"w-full\">\n      {label && (\n        <label htmlFor={textareaId} className=\"block text-sm font-medium text-gray-700 mb-1.5\">\n          {label}\n        </label>\n      )}\n      <textarea\n        id={textareaId}\n        rows={rows}\n        className={combinedClassName}\n        ref={ref}\n        {...props}\n      />\n      {error && <p className=\"mt-1.5 text-sm text-red-600\">{error}</p>}\n    </div>\n  );\n});\nTextareaFieldComponent.displayName = 'TextareaField';\n\nexport default TextareaFieldComponent;\n",
      "folder": "app/components",
      "nome_file": "TextareaFieldComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\nimport React from 'react';\nimport { Loader2 } from 'lucide-react';\n\nconst SpinnerComponent = ({ size = 'default', className = '' }) => {\n  const sizeClasses = {\n    sm: 'h-4 w-4',\n    default: 'h-8 w-8',\n    lg: 'h-12 w-12',\n  };\n\n  const combinedClassName = ['animate-spin text-blue-600', sizeClasses[size], className].filter(Boolean).join(' ');\n\n  return (\n    <div className=\"flex justify-center items-center\">\n      <Loader2 className={combinedClassName} />\n    </div>\n  );\n};\n\nexport default SpinnerComponent;\n",
      "folder": "app/components",
      "nome_file": "SpinnerComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\nimport React from 'react';\nimport { X } from 'lucide-react';\n\nconst ModalComponent = ({ isOpen, onClose, title, children, footer }) => {\n  if (!isOpen) return null;\n\n  const handleBackdropClick = (e) => {\n    if (e.target === e.currentTarget) {\n      onClose();\n    }\n  };\n\n  React.useEffect(() => {\n    const handleEscape = (event) => {\n      if (event.key === 'Escape') {\n        onClose();\n      }\n    };\n    document.addEventListener('keydown', handleEscape);\n    return () => document.removeEventListener('keydown', handleEscape);\n  }, [onClose]);\n\n  return (\n    <div \n      className=\"fixed inset-0 z-50 flex items-center justify-center bg-black bg-opacity-50 p-4\"\n      onClick={handleBackdropClick}\n    >\n      <div \n        className=\"bg-white rounded-lg shadow-xl w-full max-w-lg m-auto flex flex-col max-h-[90vh]\"\n        role=\"dialog\"\n        aria-modal=\"true\"\n        aria-labelledby=\"modal-title\"\n      >\n        <header className=\"flex items-center justify-between p-4 border-b border-gray-200\">\n          <h2 id=\"modal-title\" className=\"text-lg font-semibold text-gray-800\">{title}</h2>\n          <button \n            onClick={onClose} \n            className=\"p-1 rounded-full text-gray-500 hover:bg-gray-100 hover:text-gray-800 transition-colors\"\n            aria-label=\"Close modal\"\n          >\n            <X className=\"w-5 h-5\" />\n          </button>\n        </header>\n\n        <main className=\"p-6 overflow-y-auto\">\n          {children}\n        </main>\n\n        {footer && (\n          <footer className=\"flex justify-end p-4 border-t border-gray-200 bg-gray-50 rounded-b-lg space-x-3\">\n            {footer}\n          </footer>\n        )}\n      </div>\n    </div>\n  );\n};\n\nexport default ModalComponent;\n",
      "folder": "app/components",
      "nome_file": "ModalComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useEffect } from 'react';\nimport Link from 'next/link';\nimport { usePathname } from 'next/navigation';\nimport { Bot, Home, User, Layers, Rss, Menu, X } from 'lucide-react';\n\nconst navLinks = [\n  { href: '/', label: 'Home', icon: Home },\n  { href: '/about', label: 'About Me', icon: User },\n  { href: '/projects', label: 'Projects', icon: Layers },\n  { href: '/articles', label: 'Articles', icon: Rss },\n];\n\nconst NavbarComponent = () => {\n    const pathname = usePathname();\n    const [isMenuOpen, setIsMenuOpen] = useState(false);\n\n    useEffect(() => {\n        setIsMenuOpen(false);\n    }, [pathname]);\n\n    const NavLink = ({ href, children, isMobile = false }) => (\n        <Link href={href} className={`transition-colors hover:text-blue-600 ${pathname === href ? 'text-blue-600 font-semibold' : 'text-gray-600'} ${isMobile ? 'flex items-center gap-4 text-lg' : 'text-sm'}`}>\n            {children}\n        </Link>\n    );\n\n    return (\n        <header className=\"bg-white/95 backdrop-blur-sm sticky top-0 z-50 border-b border-gray-200\">\n            <div className=\"container mx-auto px-4 sm:px-6 lg:px-8\">\n                <div className=\"flex items-center justify-between h-16\">\n                    <Link href=\"/\" className=\"flex items-center gap-2 text-xl font-bold text-gray-800\">\n                        <Bot className=\"h-7 w-7 text-blue-600\" />\n                        <span>MyPortfolio</span>\n                    </Link>\n\n                    <nav className=\"hidden md:flex items-center gap-8 font-medium\">\n                        {navLinks.map((link) => (\n                            <NavLink key={link.href} href={link.href}>\n                                {link.label}\n                            </NavLink>\n                        ))}\n                    </nav>\n\n                    <div className=\"md:hidden\">\n                        <button onClick={() => setIsMenuOpen(!isMenuOpen)} className=\"p-2 rounded-md text-gray-600 hover:bg-gray-100 focus:outline-none focus:ring-2 focus:ring-blue-500\">\n                            {isMenuOpen ? <X className=\"h-6 w-6\"/> : <Menu className=\"h-6 w-6\" />}\n                            <span className=\"sr-only\">Toggle Menu</span>\n                        </button>\n                    </div>\n                </div>\n            </div>\n\n            {isMenuOpen && (\n                 <div className=\"md:hidden absolute top-full left-0 w-full bg-white shadow-lg border-t border-gray-200\">\n                    <nav className=\"flex flex-col p-4 space-y-4\">\n                        {navLinks.map((link) => (\n                            <NavLink key={link.href} href={link.href} isMobile={true}>\n                                <link.icon className=\"h-5 w-5\" />\n                                {link.label}\n                            </NavLink>\n                        ))}\n                    </nav>\n                </div>\n            )}\n        </header>\n    );\n};\nexport default NavbarComponent;\n",
      "folder": "app/components",
      "nome_file": "NavbarComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\nimport React from 'react';\nimport { Github, Linkedin, Twitter } from 'lucide-react';\n\nconst socialLinks = [\n  { href: '#', icon: Github, label: 'GitHub' },\n  { href: '#', icon: Linkedin, label: 'LinkedIn' },\n  { href: '#', icon: Twitter, label: 'Twitter' },\n];\n\nconst FooterComponent = () => {\n    return (\n        <footer className=\"bg-gray-50 border-t border-gray-200\">\n            <div className=\"container mx-auto px-4 sm:px-6 lg:px-8 py-8\">\n                <div className=\"flex flex-col sm:flex-row items-center justify-between gap-4\">\n                    <p className=\"text-sm text-gray-600 text-center sm:text-left\">\n                        &copy; {new Date().getFullYear()} MyPortfolio. All Rights Reserved.\n                    </p>\n                    <div className=\"flex items-center gap-x-6\">\n                        {socialLinks.map((link) => (\n                            <a \n                                key={link.label}\n                                href={link.href}\n                                target=\"_blank\"\n                                rel=\"noopener noreferrer\"\n                                className=\"text-gray-500 hover:text-blue-600 transition-colors\"\n                            >\n                                <link.icon className=\"h-5 w-5\" />\n                                <span className=\"sr-only\">{link.label}</span>\n                            </a>\n                        ))}\n                    </div>\n                </div>\n            </div>\n        </footer>\n    );\n}\n\nexport default FooterComponent;\n",
      "folder": "app/components",
      "nome_file": "FooterComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\nimport React from 'react';\nimport Link from 'next/link';\nimport { Calendar, Tag as TagIcon } from 'lucide-react';\n\nconst ArticleCardComponent = ({ article }) => {\n  const { slug, title, excerpt, created_at, tags } = article;\n  \n  const formattedDate = new Date(created_at).toLocaleDateString('en-US', {\n    year: 'numeric',\n    month: 'long',\n    day: 'numeric',\n  });\n\n  return (\n    <Link href={`/articles/${slug}`} className=\"block group h-full\">\n      <div className=\"bg-white rounded-lg shadow-sm hover:shadow-lg transition-shadow duration-300 border border-gray-200 p-6 h-full flex flex-col\">\n          <h3 className=\"text-xl font-bold text-gray-900 group-hover:text-blue-600 transition-colors mb-2\">\n            {title}\n          </h3>\n          <div className=\"flex items-center text-xs text-gray-500 mb-4\">\n              <Calendar className=\"w-4 h-4 mr-2\" />\n              <span>{formattedDate}</span>\n          </div>\n          <p className=\"text-gray-600 text-sm mb-4 flex-grow line-clamp-3\">\n            {excerpt}\n          </p>\n          {tags && tags.length > 0 && (\n            <div className=\"flex flex-wrap gap-2 mt-auto pt-4 border-t border-gray-100\">\n              {tags.map(tag => (\n                <span key={tag.slug} className=\"text-xs font-medium text-blue-800 bg-blue-100 rounded-full px-3 py-1 flex items-center gap-1\">\n                 <TagIcon className=\"w-3 h-3\" /> {tag.name}\n                </span>\n              ))}\n            </div>\n          )}\n      </div>\n    </Link>\n  );\n};\n\nexport default ArticleCardComponent;\n",
      "folder": "app/components",
      "nome_file": "ArticleCardComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "{\n  \"name\": \"blog2_4-frontend\",\n  \"version\": \"0.1.0\",\n  \"private\": true,\n  \"scripts\": {\n    \"dev\": \"next dev\",\n    \"build\": \"next build\",\n    \"start\": \"next start\",\n    \"lint\": \"next lint\"\n  },\n  \"dependencies\": {\n    \"@tailwindcss/typography\": \"^0.5.13\",\n    \"axios\": \"^1.7.2\",\n    \"class-variance-authority\": \"^0.7.0\",\n    \"clsx\": \"^2.1.1\",\n    \"lucide-react\": \"^0.395.0\",\n    \"next\": \"14.2.4\",\n    \"react\": \"^18\",\n    \"react-dom\": \"^18\",\n    \"react-markdown\": \"^9.0.1\",\n    \"react-mde\": \"^11.5.0\"\n  },\n  \"devDependencies\": {\n    \"postcss\": \"^8\",\n    \"tailwindcss\": \"^3.4.1\",\n    \"tailwindcss-animate\": \"^1.0.7\"\n  }\n}\n",
      "folder": "",
      "nome_file": "package.json",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React from 'react';\nimport Link from 'next/link';\nimport { ArrowRight, Layers } from 'lucide-react';\n\nconst ProjectCardComponent = ({ project }) => {\n  const { slug, title, excerpt, images } = project;\n  // The API sends back simplified project data for the list view, so we adapt\n  const imageUrl = images && images.length > 0 ? images[0].url : project.image_url;\n\n  const fullImageUrl = imageUrl ? `http://localhost:5000${imageUrl}` : null;\n\n  return (\n    <Link href={`/projects/${slug}`} className=\"block group h-full\">\n      <div className=\"bg-white rounded-xl shadow-md hover:shadow-xl transition-all duration-300 border border-gray-200/80 overflow-hidden h-full flex flex-col\">\n        <div className=\"relative aspect-video overflow-hidden\">\n          {fullImageUrl ? (\n            <img\n              src={fullImageUrl}\n              alt={title}\n              className=\"w-full h-full object-cover group-hover:scale-105 transition-transform duration-300\"\n            />\n          ) : (\n            <div className=\"w-full h-full bg-gray-100 flex items-center justify-center\">\n              <Layers className=\"w-12 h-12 text-gray-400\" />\n            </div>\n          )}\n           <div className=\"absolute inset-0 bg-black/10 group-hover:bg-black/20 transition-colors duration-300\"></div>\n        </div>\n        <div className=\"p-6 flex-grow flex flex-col\">\n          <h3 className=\"text-xl font-bold text-gray-800 group-hover:text-blue-600 transition-colors duration-200\">{title}</h3>\n          <p className=\"text-gray-600 text-sm mt-2 mb-6 flex-grow line-clamp-3\">{excerpt}</p>\n          <div className=\"mt-auto\">\n            <span className=\"text-sm font-semibold text-blue-600 group-hover:underline flex items-center\">\n              View Project Details\n              <ArrowRight className=\"w-4 h-4 ml-1.5 transition-transform group-hover:translate-x-1\" />\n            </span>\n          </div>\n        </div>\n      </div>\n    </Link>\n  );\n};\n\nexport default ProjectCardComponent;\n",
      "folder": "app/components",
      "nome_file": "ProjectCardComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React from 'react';\nimport ReactMarkdown from 'react-markdown';\n\nconst MarkdownRendererComponent = ({ content }) => {\n  return (\n    <div className=\"prose prose-lg lg:prose-xl max-w-none prose-h1:font-extrabold prose-h2:font-bold prose-a:text-blue-600 prose-a:no-underline hover:prose-a:underline prose-code:bg-gray-100 prose-code:p-1 prose-code:rounded-md prose-code:font-mono prose-code:text-sm prose-pre:bg-gray-800 prose-pre:text-gray-100 prose-pre:p-4 prose-pre:rounded-lg text-gray-700\">\n      <ReactMarkdown>{content}</ReactMarkdown>\n    </div>\n  );\n};\n\nexport default MarkdownRendererComponent;\n",
      "folder": "app/components",
      "nome_file": "MarkdownRendererComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React from 'react';\nimport Link from 'next/link';\nimport { useRouter, usePathname } from 'next/navigation';\nimport { useAuth } from '../../context/AuthContext';\nimport { LayoutDashboard, FileText, Layers, Settings, LogOut, Bot } from 'lucide-react';\n\nconst navItems = [\n    { href: '/admin/dashboard', icon: LayoutDashboard, label: 'Dashboard' },\n    { href: '/admin/articles', icon: FileText, label: 'Articles' },\n    { href: '/admin/projects', icon: Layers, label: 'Projects' },\n    { href: '/admin/settings', icon: Settings, label: 'Settings' },\n];\n\nconst AdminSidebarComponent = () => {\n    const pathname = usePathname();\n    const router = useRouter();\n    const { logout } = useAuth();\n\n    const handleLogout = () => {\n        logout();\n        router.push('/admin/login');\n    };\n\n    return (\n        <aside className=\"w-64 flex-shrink-0 bg-gray-900 text-white flex flex-col\">\n            <div className=\"h-16 flex items-center justify-center px-4 border-b border-gray-700/50\">\n                <Link href=\"/\" className=\"flex items-center gap-2 text-xl font-bold\">\n                    <Bot className=\"h-7 w-7 text-blue-400\" />\n                    <span>MyPortfolio</span>\n                </Link>\n            </div>\n            <nav className=\"flex-1 p-4 space-y-2\">\n                {navItems.map(item => (\n                    <Link key={item.href} href={item.href}\n                          className={`flex items-center px-3 py-2.5 text-sm font-medium rounded-md transition-colors ${pathname.startsWith(item.href) ? 'bg-blue-600 text-white' : 'text-gray-300 hover:bg-gray-700/50 hover:text-white'}`}>\n                        <item.icon className=\"w-5 h-5 mr-3 flex-shrink-0\" />\n                        <span>{item.label}</span>\n                    </Link>\n                ))}\n            </nav>\n            <div className=\"p-4 border-t border-gray-700/50\">\n                 <button onClick={handleLogout} className=\"flex w-full items-center px-3 py-2.5 text-sm font-medium rounded-md text-red-400 hover:bg-red-500/20 hover:text-red-300 transition-colors\">\n                    <LogOut className=\"w-5 h-5 mr-3\"/>\n                    <span>Logout</span>\n                </button>\n            </div>\n        </aside>\n    );\n};\n\nexport default AdminSidebarComponent;\n",
      "folder": "app/components/admin",
      "nome_file": "AdminSidebarComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React, { useState, useCallback } from 'react';\nimport { UploadCloud, File, X, Loader2 } from 'lucide-react';\n\nconst ImageUploaderComponent = ({ onUploadSuccess }) => {\n  const [preview, setPreview] = useState(null);\n  const [file, setFile] = useState(null);\n  const [status, setStatus] = useState('idle'); // idle, uploading, success, error\n  const [error, setError] = useState('');\n\n  const handleFileChange = (selectedFile) => {\n    if (selectedFile && selectedFile.type.startsWith('image/')) {\n      setFile(selectedFile);\n      setPreview(URL.createObjectURL(selectedFile));\n      handleUpload(selectedFile);\n    }\n  };\n\n  const onDrop = useCallback((event) => {\n    event.preventDefault();\n    handleFileChange(event.dataTransfer.files[0]);\n  }, []);\n\n  const onDragOver = (event) => {\n    event.preventDefault();\n  };\n\n  const handleUpload = async (fileToUpload) => {\n    setStatus('uploading');\n    setError('');\n    const token = localStorage.getItem('jwt_token');\n    const formData = new FormData();\n    formData.append('image', fileToUpload);\n\n    try {\n      const response = await fetch('http://localhost:5000/api/admin/upload-image', {\n        method: 'POST',\n        headers: { 'Authorization': `Bearer ${token}` },\n        body: formData,\n      });\n\n      if (!response.ok) {\n        throw new Error('Upload failed');\n      }\n      const data = await response.json();\n      onUploadSuccess(data.url); \n      setStatus('success');\n    } catch (err) {\n      setStatus('error');\n      setError(err.message);\n    }\n  };\n\n  const removeImage = () => {\n    setFile(null);\n    setPreview(null);\n    setStatus('idle');\n  };\n\n  return (\n    <div className=\"w-full\">\n      <div\n        className=\"relative flex flex-col items-center justify-center w-full p-6 border-2 border-dashed border-gray-300 rounded-lg cursor-pointer hover:border-blue-500 transition-colors bg-gray-50/50\"\n        onDrop={onDrop}\n        onDragOver={onDragOver}\n        onClick={() => document.getElementById('image-upload-input').click()}\n      >\n        {status === 'uploading' && <Loader2 className=\"h-8 w-8 animate-spin text-blue-500\" />}\n        {status === 'idle' && !preview && (\n          <>\n            <UploadCloud className=\"w-10 h-10 text-gray-400 mb-2\" />\n            <p className=\"text-sm text-gray-600\">Drag & drop an image or <span className=\"font-semibold text-blue-600\">click to upload</span></p>\n          </>\n        )}\n        {preview && status !== 'uploading' && (\n          <img src={preview} alt=\"Preview\" className=\"max-h-32 rounded-md object-contain\" />\n        )}\n        <input id=\"image-upload-input\" type=\"file\" accept=\"image/*\" className=\"hidden\" onChange={(e) => handleFileChange(e.target.files[0])} />\n      </div>\n      {preview && status !== 'uploading' && (\n        <div className=\"mt-2 flex items-center justify-between p-2 bg-gray-100 rounded-md\">\n          <div className=\"flex items-center gap-2 text-sm text-gray-700\">\n            <File className=\"w-4 h-4\" /> {file.name}\n          </div>\n          <button onClick={removeImage} className=\"p-1 text-gray-500 hover:text-red-600 rounded-full hover:bg-gray-200 transition-colors\">\n            <X className=\"w-4 h-4\" />\n          </button>\n        </div>\n      )}\n      {status === 'error' && <p className=\"text-red-500 text-sm mt-2\">Error: {error}</p>}\n    </div>\n  );\n};\n\nexport default ImageUploaderComponent;\n",
      "folder": "app/components/admin",
      "nome_file": "ImageUploaderComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React, { useState } from 'react';\nimport ReactMde from 'react-mde';\nimport ReactMarkdown from 'react-markdown';\nimport 'react-mde/lib/styles/css/react-mde-all.css';\n\nconst MarkdownEditorComponent = ({ value, onChange }) => {\n  const [selectedTab, setSelectedTab] = useState('write');\n\n  return (\n    <div className=\"w-full prose-headings:text-gray-900 prose-p:text-gray-800\">\n      <ReactMde\n        value={value}\n        onChange={onChange}\n        selectedTab={selectedTab}\n        onTabChange={setSelectedTab}\n        generateMarkdownPreview={(markdown) =>\n          Promise.resolve(<ReactMarkdown>{markdown}</ReactMarkdown>)\n        }\n        childProps={{\n          writeButton: {\n            tabIndex: -1\n          }\n        }}\n      />\n    </div>\n  );\n};\n\nexport default MarkdownEditorComponent;\n",
      "folder": "app/components/admin",
      "nome_file": "MarkdownEditorComponent.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React, { createContext, useContext, useState, useEffect } from 'react';\n\nconst AuthContext = createContext(null);\n\nexport const AuthProvider = ({ children }) => {\n    const [token, setToken] = useState(null);\n    const [loading, setLoading] = useState(true);\n\n    useEffect(() => {\n        const storedToken = localStorage.getItem('jwt_token');\n        if (storedToken) {\n            setToken(storedToken);\n        }\n        setLoading(false);\n    }, []);\n\n    const login = async (username, password) => {\n        const response = await fetch('http://localhost:5000/api/auth/login', {\n            method: 'POST',\n            headers: { 'Content-Type': 'application/json' },\n            body: JSON.stringify({ username, password }),\n        });\n\n        if (!response.ok) {\n            const errorData = await response.json();\n            throw new Error(errorData.message || 'Login failed');\n        }\n\n        const data = await response.json();\n        localStorage.setItem('jwt_token', data.access_token);\n        setToken(data.access_token);\n        return data;\n    };\n\n    const logout = () => {\n        localStorage.removeItem('jwt_token');\n        setToken(null);\n    };\n\n    const value = {\n        token,\n        isAuthenticated: !!token,\n        loading,\n        login,\n        logout,\n    };\n\n    return (\n        <AuthContext.Provider value={value}>\n            {children}\n        </AuthContext.Provider>\n    );\n};\n\nexport const useAuth = () => {\n    const context = useContext(AuthContext);\n    if (context === undefined) {\n        throw new Error('useAuth must be used within an AuthProvider');\n    }\n    return context;\n};\n",
      "folder": "app/context",
      "nome_file": "AuthContext.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React from 'react';\nimport NavbarComponent from '../../components/NavbarComponent';\nimport FooterComponent from '../../components/FooterComponent';\n\nconst PublicLayout = ({ children }) => {\n  return (\n    <div className=\"flex flex-col min-h-screen bg-gray-50 text-gray-700\">\n      <NavbarComponent />\n      <main className=\"flex-grow container mx-auto px-4 sm:px-6 lg:px-8 py-8 md:py-12\">\n        {children}\n      </main>\n      <FooterComponent />\n    </div>\n  );\n};\n\nexport default PublicLayout;\n",
      "folder": "app/(public)",
      "nome_file": "layout.jsx",
      "categoria": "componente"
    },
    {
      "codice": "'use client';\n\nimport React, { useEffect } from 'react';\nimport { useRouter } from 'next/navigation';\nimport { useAuth } from '../context/AuthContext';\nimport AdminSidebarComponent from '../components/admin/AdminSidebarComponent';\nimport SpinnerComponent from '../components/SpinnerComponent';\n\nconst AdminLayout = ({ children }) => {\n    const { isAuthenticated, loading } = useAuth();\n    const router = useRouter();\n\n    useEffect(() => {\n        if (!loading && !isAuthenticated) {\n            router.replace('/admin/login');\n        }\n    }, [isAuthenticated, loading, router]);\n\n    if (loading || !isAuthenticated) {\n        return (\n            <div className=\"flex items-center justify-center min-h-screen bg-gray-100\">\n                <SpinnerComponent size=\"lg\" />\n            </div>\n        );\n    }\n\n    return (\n        <div className=\"flex min-h-screen bg-gray-100\">\n            <AdminSidebarComponent />\n            <div className=\"flex-1 flex flex-col\">\n                <main className=\"flex-1 p-6 lg:p-8\">\n                    {children}\n                </main>\n            </div>\n        </div>\n    );\n};\n\nexport default AdminLayout;\n",
      "folder": "app/admin",
      "nome_file": "layout.jsx",
      "categoria": "componente"
    }
  ]
}
